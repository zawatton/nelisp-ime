;;; go-ping-pong-cps.el --- GENERATED: build-time CPS transform of nl-clj-async's go/chan demo -*- lexical-binding: t; -*-

;; SPDX-License-Identifier: GPL-3.0-or-later

;;; Commentary:

;; GENERATED by `packages/nl-clj/scripts/nl-clj-async-cps-dump.el'
;; (run under Emacs 30.1) from `nl-clj-async.el's `nl-clj-async--make-
;; chan-1'/`nl-clj-async--blocking-take-1'/`nl-clj-async--blocking-
;; put-1' and `examples/nl-clj-async/go-ping-pong.el's two repeated-
;; resumption fixtures plus `nl-clj-async-demo-ping-pong'.  Doc 195
;; §4.6.
;;
;; Every `nl-clj-go'/`nelisp-actor-lambda' form has been replaced by
;; its `macroexpand-all' output under REAL `generator.el': ordinary
;; closures implementing the CPS state machine directly.  No
;; `iter-lambda'/`iter-yield'/`nl-clj-go' macro call remains anywhere
;; in this file -- `nelisp-spawn' just `funcall's whatever thunk it is
;; given and does not care how the resulting iterator was built, so
;; these plug into `nelisp-actor.el' exactly as the macro-built ones
;; do, and run on the standalone substrate via the `iter-next'/`iter-
;; close'/`iter-end-of-sequence' shim `nelisp-actor.el' installs when
;; `generator.el' is absent.
;;
;; DO NOT EDIT BY HAND.  Regenerate with `make nl-clj-async-cps-baseline'.

;;; Code:

(defun nl-clj-async--make-chan-1-standalone (capacity)
  "The real, un-baked channel-mediator constructor.  See this file's\nCommentary for why this is not literally `nelisp-make-chan'.  CAPACITY\nnil or 0 gives an unbuffered (rendezvous) channel; a positive integer\npermits that many values to queue without blocking a sender -- same\ncontract as `nelisp-make-chan'.  Returns an ordinary `nelisp-chan'\nstruct (from nelisp-actor.el); `nelisp-chan-send'/`nelisp-chan-recv'/\n`nelisp-chan-close' -- and this file's own `nl-clj->!'/`nl-clj-<!'/\n`nl-clj-close!', which are exactly those -- work on the result exactly\nas they do on a `nelisp-make-chan' channel, since\n`nl-clj-async--chan-dispatch's `:send'/`:recv'/`:close' handling is\nbyte-identical to that mediator's own.  `:recv-alt'/`:send-alt' are\nthe two new message types this package's own alts! design (Doc 195\n§4.6) adds, claim-checked through `nl-clj-async--alts-claim' (`nl-safe''s\n`nl-cell' reused as a claim-once race adjudicator, per that section's\nown text).  The actor body itself is deliberately tiny -- see\n`nl-clj-async--chan-state'/`nl-clj-async--chan-dispatch' above for why:\nthis is the ONE `nelisp-actor-lambda' this file's standalone bake\nneeds to transform (`nl-clj-async-cps-baseline'), and a small body is\nboth easier to verify by hand and a smaller CPS-transform surface.\n\nAgainst-the-bug, ONE deliberate divergence from `nelisp-make-chan'\nfound (and fixed here, not inherited) by this package's own test\nsuite: `nelisp-make-chan''s own mediator loop condition -- `(while\n(not (and closed (null buf) (null pending-senders))))' -- lets the\nactor terminate (`:dead') the moment a channel is closed AND has\nnothing buffered or pending.  That is fine for every existing\n`nelisp-actor-test.el' channel case (none of them `:recv'/`:send' a\nSECOND time after that point), but it directly breaks Clojure's own\n`<!'/`>!' contract on a closed channel: a real closed channel answers\n`<!' with nil, and `>!' with false, REPEATEDLY, forever -- not once.\nA dead mediator cannot do that; the very next `nelisp-chan-recv' on it\nsignals `nelisp-actor-error' `send-to-dead-actor' instead of returning\n`(:closed)' -- measured directly, running this package's own close!\ntests before this fix: closing an unbuffered channel with nothing\npending killed the mediator immediately, and a `nl-clj-<!' called on\nit even once more crashed the caller.  The fix: this actor's own loop\nnever exits (`(while t ...)') -- `closed' is state the dispatch\nhandlers already consult on every request (see `nl-clj-async--chan-\nhandle-recv'/`-send'/`-recv-alt'/`-send-alt' above), not a shutdown\nsignal, so serving `(:closed)'/`:error-closed' indefinitely falls out\nof the existing per-request logic with no new code; only the loop's\nown willingness to keep asking `(nelisp-receive)' for one more request\nhad to change.  The mediator actor for a channel therefore lives for\nas long as the channel itself is reachable, exactly matching a real\ncore.async channel's own lifecycle (an object that keeps answering\ncorrectly for its whole lifetime, not one that vanishes on close)."
  (let*
      ((cap (or capacity 0))
       (act
	(nelisp-spawn
	 #'(lambda nil
	     (let
		 (cps-current-state-1 cps-current-value-0
				      cps-state-terminal-2
				      cps-binding-state-3
				      cps-state-atom-5
				      cps-binding-cps-argument-6-7
				      cps-state-atom-8
				      cps-state-let*-9
				      cps-state-atom-10
				      cps-state-atom-12
				      cps-state-iter-yield-13
				      cps-state-atom-14
				      cps-state-atom-15
				      cps-state-while-11
				      cps-state-while-4
				      cps-state-let*-16
				      cps-state-atom-17)
	       (setq cps-state-terminal-2
		     #'(lambda nil
			 (signal 'iter-end-of-sequence
				 cps-current-value-0)))
	       (setq cps-state-atom-5
		     #'(lambda nil
			 (setq cps-current-value-0
			       (prog1 t
				 (setq cps-current-state-1
				       cps-state-while-4)))))
	       (setq cps-state-atom-8
		     #'(lambda nil
			 (setq cps-current-value-0
			       (prog1
				   (let* nil
				     (nl-clj-async--chan-dispatch
				      cps-binding-state-3 cap
				      cps-binding-cps-argument-6-7))
				 (setq cps-current-state-1
				       cps-state-atom-5)))))
	       (setq cps-state-let*-9
		     #'(lambda nil
			 (progn
			   (setq cps-binding-cps-argument-6-7
				 cps-current-value-0)
			   (setq cps-current-state-1 cps-state-atom-8))))
	       (setq cps-state-atom-10
		     #'(lambda nil
			 (setq cps-current-value-0
			       (prog1
				   (progn
				     (nelisp-actor--mailbox-pop
				      nelisp-actor--current))
				 (setq cps-current-state-1
				       cps-state-let*-9)))))
	       (setq cps-state-atom-12
		     #'(lambda nil
			 (setq cps-current-value-0
			       (prog1
				   (null
				    (nelisp-actor-mailbox
				     nelisp-actor--current))
				 (setq cps-current-state-1
				       cps-state-while-11)))))
	       (setq cps-state-iter-yield-13
		     #'(lambda nil
			 (progn
			   (setq cps-current-state-1 cps-state-atom-12)
			   (throw 'cps--yield cps-current-value-0))))
	       (setq cps-state-atom-14
		     #'(lambda nil
			 (setq cps-current-value-0
			       (prog1 'nelisp-actor--receive
				 (setq cps-current-state-1
				       cps-state-iter-yield-13)))))
	       (setq cps-state-atom-15
		     #'(lambda nil
			 (setq cps-current-value-0
			       (prog1
				   (let* ((v nelisp-actor--current))
				     (nelisp-actor--set-status v
							       :blocked-receive))
				 (setq cps-current-state-1
				       cps-state-atom-14)))))
	       (setq cps-state-while-11
		     #'(lambda nil
			 (progn
			   (setq cps-current-state-1
				 (if cps-current-value-0
				     cps-state-atom-15
				   cps-state-atom-10)))))
	       (setq cps-state-while-4
		     #'(lambda nil
			 (progn
			   (setq cps-current-state-1
				 (if cps-current-value-0
				     cps-state-atom-12
				   cps-state-terminal-2)))))
	       (setq cps-state-let*-16
		     #'(lambda nil
			 (progn
			   (setq cps-binding-state-3
				 cps-current-value-0)
			   (setq cps-current-state-1 cps-state-atom-5))))
	       (setq cps-state-atom-17
		     #'(lambda nil
			 (setq cps-current-value-0
			       (prog1 (nl-clj-async--chan-state--make)
				 (setq cps-current-state-1
				       cps-state-let*-16)))))
	       (setq cps-current-state-1 cps-state-atom-17)
	       (let
		   ((iterator
		     #'(lambda (op value)
			 (cond
			  ((eq op :close)
			   (progn
			     (setq cps-current-state-1
				   cps-state-terminal-2)
			     (setq cps-current-value-0 nil)))
			  ((eq op :next)
			   (setq cps-current-value-0 value)
			   (let ((yielded nil))
			     (unwind-protect
				 (prog1
				     (catch 'cps--yield
				       (while t
					 (funcall cps-current-state-1)))
				   (setq yielded t))
			       (if yielded nil
				 (progn
				   (setq cps-current-state-1
					 cps-state-terminal-2)
				   (setq cps-current-value-0 nil))))))
			  (t
			   (error "Unknown iterator operation %S" op))))))
		 nil iterator))))))
    (nelisp-chan--make :actor act :capacity cap)))

(defun nl-clj-async--blocking-take-1-standalone (chan)
  "The real, un-baked body of `nl-clj-<!!'.  See that function and this\nfile's Commentary."
  (let (result got)
    (nelisp-spawn
     (let ((chan chan))
       #'(lambda nil
	   (let
	       (cps-current-state-19 cps-current-value-18
				     cps-state-terminal-20
				     cps-binding-v-21
				     cps-state-atom-22
				     cps-state-let*-23
				     cps-state-atom-24
				     cps-state-atom-26
				     cps-state-iter-yield-27
				     cps-state-atom-28
				     cps-state-atom-29
				     cps-state-while-25
				     cps-state-atom-30)
	     (setq cps-state-terminal-20
		   #'(lambda nil
		       (signal 'iter-end-of-sequence
			       cps-current-value-18)))
	     (setq cps-state-atom-22
		   #'(lambda nil
		       (setq cps-current-value-18
			     (prog1
				 (let* nil
				   (progn
				     (setq result
					   (if
					       (eq
						(car cps-binding-v-21)
						:closed)
					       nil
					     (car
					      (cdr cps-binding-v-21))))
				     (setq got t)))
			       (setq cps-current-state-19
				     cps-state-terminal-20)))))
	     (setq cps-state-let*-23
		   #'(lambda nil
		       (progn
			 (setq cps-binding-v-21 cps-current-value-18)
			 (setq cps-current-state-19 cps-state-atom-22))))
	     (setq cps-state-atom-24
		   #'(lambda nil
		       (setq cps-current-value-18
			     (prog1
				 (progn
				   (nelisp-actor--mailbox-pop
				    nelisp-actor--current))
			       (setq cps-current-state-19
				     cps-state-let*-23)))))
	     (setq cps-state-atom-26
		   #'(lambda nil
		       (setq cps-current-value-18
			     (prog1
				 (null
				  (nelisp-actor-mailbox
				   nelisp-actor--current))
			       (setq cps-current-state-19
				     cps-state-while-25)))))
	     (setq cps-state-iter-yield-27
		   #'(lambda nil
		       (progn
			 (setq cps-current-state-19 cps-state-atom-26)
			 (throw 'cps--yield cps-current-value-18))))
	     (setq cps-state-atom-28
		   #'(lambda nil
		       (setq cps-current-value-18
			     (prog1 'nelisp-actor--receive
			       (setq cps-current-state-19
				     cps-state-iter-yield-27)))))
	     (setq cps-state-atom-29
		   #'(lambda nil
		       (setq cps-current-value-18
			     (prog1
				 (let* ((v nelisp-actor--current))
				   (nelisp-actor--set-status v
							     :blocked-receive))
			       (setq cps-current-state-19
				     cps-state-atom-28)))))
	     (setq cps-state-while-25
		   #'(lambda nil
		       (progn
			 (setq cps-current-state-19
			       (if cps-current-value-18
				   cps-state-atom-29
				 cps-state-atom-24)))))
	     (setq cps-state-atom-30
		   #'(lambda nil
		       (setq cps-current-value-18
			     (prog1
				 (nelisp-send (nelisp-chan-actor chan)
					      (list :recv
						    (nelisp-self)))
			       (setq cps-current-state-19
				     cps-state-atom-26)))))
	     (setq cps-current-state-19 cps-state-atom-30)
	     (let
		 ((iterator
		   #'(lambda (op value)
		       (cond
			((eq op :close)
			 (progn
			   (setq cps-current-state-19
				 cps-state-terminal-20)
			   (setq cps-current-value-18 nil)))
			((eq op :next)
			 (setq cps-current-value-18 value)
			 (let ((yielded nil))
			   (unwind-protect
			       (prog1
				   (catch 'cps--yield
				     (while t
				       (funcall cps-current-state-19)))
				 (setq yielded t))
			     (if yielded nil
			       (progn
				 (setq cps-current-state-19
				       cps-state-terminal-20)
				 (setq cps-current-value-18 nil))))))
			(t (error "Unknown iterator operation %S" op))))))
	       nil iterator)))))
    (nelisp-actor-run-until-idle)
    (unless got
      (signal 'nl-clj-async-error
	      (list 'nl-clj-<!!
		    "channel never became ready; scheduler idle with no runnable actor left to satisfy it"
		    chan)))
    result))

(defun nl-clj-async--blocking-put-1-standalone (chan val)
  "The real, un-baked body of `nl-clj->!!'.  See that function and this\nfile's Commentary."
  (let (result got)
    (nelisp-spawn
     (let ((chan chan) (val val))
       #'(lambda nil
	   (let
	       (cps-current-state-32 cps-current-value-31
				     cps-state-terminal-33
				     cps-state-atom-34
				     cps-binding-cps-argument-35-36
				     cps-state-atom-37
				     cps-state-let*-38
				     cps-binding-cps-argument-39-40
				     cps-state-atom-41
				     cps-state-let*-42
				     cps-binding-cps-argument-43-44
				     cps-state-atom-45
				     cps-state-let*-46
				     cps-state-atom-47
				     cps-state-atom-49
				     cps-state-iter-yield-50
				     cps-state-atom-51
				     cps-state-atom-52
				     cps-state-while-48
				     cps-state-atom-53)
	     (setq cps-state-terminal-33
		   #'(lambda nil
		       (signal 'iter-end-of-sequence
			       cps-current-value-31)))
	     (setq cps-state-atom-34
		   #'(lambda nil
		       (setq cps-current-value-31
			     (prog1 (progn (setq got t))
			       (setq cps-current-state-32
				     cps-state-terminal-33)))))
	     (setq cps-state-atom-37
		   #'(lambda nil
		       (setq cps-current-value-31
			     (prog1
				 (let* nil
				   (setq result
					 cps-binding-cps-argument-35-36))
			       (setq cps-current-state-32
				     cps-state-atom-34)))))
	     (setq cps-state-let*-38
		   #'(lambda nil
		       (progn
			 (setq cps-binding-cps-argument-35-36
			       cps-current-value-31)
			 (setq cps-current-state-32 cps-state-atom-37))))
	     (setq cps-state-atom-41
		   #'(lambda nil
		       (setq cps-current-value-31
			     (prog1
				 (let* nil
				   (not cps-binding-cps-argument-39-40))
			       (setq cps-current-state-32
				     cps-state-let*-38)))))
	     (setq cps-state-let*-42
		   #'(lambda nil
		       (progn
			 (setq cps-binding-cps-argument-39-40
			       cps-current-value-31)
			 (setq cps-current-state-32 cps-state-atom-41))))
	     (setq cps-state-atom-45
		   #'(lambda nil
		       (setq cps-current-value-31
			     (prog1
				 (let* nil
				   (eq cps-binding-cps-argument-43-44
				       :error-closed))
			       (setq cps-current-state-32
				     cps-state-let*-42)))))
	     (setq cps-state-let*-46
		   #'(lambda nil
		       (progn
			 (setq cps-binding-cps-argument-43-44
			       cps-current-value-31)
			 (setq cps-current-state-32 cps-state-atom-45))))
	     (setq cps-state-atom-47
		   #'(lambda nil
		       (setq cps-current-value-31
			     (prog1
				 (progn
				   (nelisp-actor--mailbox-pop
				    nelisp-actor--current))
			       (setq cps-current-state-32
				     cps-state-let*-46)))))
	     (setq cps-state-atom-49
		   #'(lambda nil
		       (setq cps-current-value-31
			     (prog1
				 (null
				  (nelisp-actor-mailbox
				   nelisp-actor--current))
			       (setq cps-current-state-32
				     cps-state-while-48)))))
	     (setq cps-state-iter-yield-50
		   #'(lambda nil
		       (progn
			 (setq cps-current-state-32 cps-state-atom-49)
			 (throw 'cps--yield cps-current-value-31))))
	     (setq cps-state-atom-51
		   #'(lambda nil
		       (setq cps-current-value-31
			     (prog1 'nelisp-actor--receive
			       (setq cps-current-state-32
				     cps-state-iter-yield-50)))))
	     (setq cps-state-atom-52
		   #'(lambda nil
		       (setq cps-current-value-31
			     (prog1
				 (let* ((v nelisp-actor--current))
				   (nelisp-actor--set-status v
							     :blocked-receive))
			       (setq cps-current-state-32
				     cps-state-atom-51)))))
	     (setq cps-state-while-48
		   #'(lambda nil
		       (progn
			 (setq cps-current-state-32
			       (if cps-current-value-31
				   cps-state-atom-52
				 cps-state-atom-47)))))
	     (setq cps-state-atom-53
		   #'(lambda nil
		       (setq cps-current-value-31
			     (prog1
				 (nelisp-send (nelisp-chan-actor chan)
					      (list :send val
						    (nelisp-self)))
			       (setq cps-current-state-32
				     cps-state-atom-49)))))
	     (setq cps-current-state-32 cps-state-atom-53)
	     (let
		 ((iterator
		   #'(lambda (op value)
		       (cond
			((eq op :close)
			 (progn
			   (setq cps-current-state-32
				 cps-state-terminal-33)
			   (setq cps-current-value-31 nil)))
			((eq op :next)
			 (setq cps-current-value-31 value)
			 (let ((yielded nil))
			   (unwind-protect
			       (prog1
				   (catch 'cps--yield
				     (while t
				       (funcall cps-current-state-32)))
				 (setq yielded t))
			     (if yielded nil
			       (progn
				 (setq cps-current-state-32
				       cps-state-terminal-33)
				 (setq cps-current-value-31 nil))))))
			(t (error "Unknown iterator operation %S" op))))))
	       nil iterator)))))
    (nelisp-actor-run-until-idle)
    (unless got
      (signal 'nl-clj-async-error
	      (list 'nl-clj->!!
		    "channel put never completed; scheduler idle with no runnable actor left to satisfy it"
		    chan val)))
    result))

(defun nl-clj-async-demo-repeated-take-standalone nil
  "Exercise one `nl-clj-<!' park point twice from the same `while'.\nThe producer deliberately uses the raw `nelisp-chan-send' primitive,\nso this is a minimal take-side reproducer independent of `nl-clj-go',\n`nl-clj->!', channel close, and conditional branching."
  (nelisp-actor--reset)
  (let ((ch (nl-clj-chan)) (seen nil))
    (nelisp-spawn
     #'(lambda nil
	 (let
	     (cps-current-state-55 cps-current-value-54
				   cps-state-terminal-56
				   cps-binding-i-57 cps-state-atom-59
				   cps-state-atom-60 cps-binding-v-61
				   cps-state-atom-62 cps-state-let*-63
				   cps-binding-cps-argument-64-65
				   cps-state-atom-66 cps-state-let*-67
				   cps-state-atom-68 cps-state-atom-70
				   cps-state-iter-yield-71
				   cps-state-atom-72 cps-state-atom-73
				   cps-state-while-69
				   cps-state-atom-74
				   cps-state-while-58
				   cps-state-let*-75 cps-state-atom-76)
	   (setq cps-state-terminal-56
		 #'(lambda nil
		     (signal 'iter-end-of-sequence
			     cps-current-value-54)))
	   (setq cps-state-atom-59
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1 (< cps-binding-i-57 2)
			     (setq cps-current-state-55
				   cps-state-while-58)))))
	   (setq cps-state-atom-60
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1
			       (progn
				 (setq cps-binding-i-57
				       (1+ cps-binding-i-57)))
			     (setq cps-current-state-55
				   cps-state-atom-59)))))
	   (setq cps-state-atom-62
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1
			       (let* nil
				 (setq seen
				       (cons cps-binding-v-61 seen)))
			     (setq cps-current-state-55
				   cps-state-atom-60)))))
	   (setq cps-state-let*-63
		 #'(lambda nil
		     (progn
		       (setq cps-binding-v-61 cps-current-value-54)
		       (setq cps-current-state-55 cps-state-atom-62))))
	   (setq cps-state-atom-66
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1
			       (let* nil
				 (nl-clj-async--unwrap-recv
				  cps-binding-cps-argument-64-65))
			     (setq cps-current-state-55
				   cps-state-let*-63)))))
	   (setq cps-state-let*-67
		 #'(lambda nil
		     (progn
		       (setq cps-binding-cps-argument-64-65
			     cps-current-value-54)
		       (setq cps-current-state-55 cps-state-atom-66))))
	   (setq cps-state-atom-68
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1
			       (progn
				 (nelisp-actor--mailbox-pop
				  nelisp-actor--current))
			     (setq cps-current-state-55
				   cps-state-let*-67)))))
	   (setq cps-state-atom-70
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1
			       (null
				(nelisp-actor-mailbox
				 nelisp-actor--current))
			     (setq cps-current-state-55
				   cps-state-while-69)))))
	   (setq cps-state-iter-yield-71
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-55 cps-state-atom-70)
		       (throw 'cps--yield cps-current-value-54))))
	   (setq cps-state-atom-72
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1 'nelisp-actor--receive
			     (setq cps-current-state-55
				   cps-state-iter-yield-71)))))
	   (setq cps-state-atom-73
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1
			       (let* ((v nelisp-actor--current))
				 (nelisp-actor--set-status v
							   :blocked-receive))
			     (setq cps-current-state-55
				   cps-state-atom-72)))))
	   (setq cps-state-while-69
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-55
			     (if cps-current-value-54
				 cps-state-atom-73
			       cps-state-atom-68)))))
	   (setq cps-state-atom-74
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1
			       (nelisp-send (nelisp-chan-actor ch)
					    (list :recv (nelisp-self)))
			     (setq cps-current-state-55
				   cps-state-atom-70)))))
	   (setq cps-state-while-58
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-55
			     (if cps-current-value-54
				 cps-state-atom-74
			       cps-state-terminal-56)))))
	   (setq cps-state-let*-75
		 #'(lambda nil
		     (progn
		       (setq cps-binding-i-57 cps-current-value-54)
		       (setq cps-current-state-55 cps-state-atom-59))))
	   (setq cps-state-atom-76
		 #'(lambda nil
		     (setq cps-current-value-54
			   (prog1 0
			     (setq cps-current-state-55
				   cps-state-let*-75)))))
	   (setq cps-current-state-55 cps-state-atom-76)
	   (let
	       ((iterator
		 #'(lambda (op value)
		     (cond
		      ((eq op :close)
		       (progn
			 (setq cps-current-state-55
			       cps-state-terminal-56)
			 (setq cps-current-value-54 nil)))
		      ((eq op :next) (setq cps-current-value-54 value)
		       (let ((yielded nil))
			 (unwind-protect
			     (prog1
				 (catch 'cps--yield
				   (while t
				     (funcall cps-current-state-55)))
			       (setq yielded t))
			   (if yielded nil
			     (progn
			       (setq cps-current-state-55
				     cps-state-terminal-56)
			       (setq cps-current-value-54 nil))))))
		      (t (error "Unknown iterator operation %S" op))))))
	     nil iterator))))
    (nelisp-spawn
     #'(lambda nil
	 (let
	     (cps-current-state-78 cps-current-value-77
				   cps-state-terminal-79
				   cps-binding---ack---80
				   cps-state-atom-81 cps-state-let*-82
				   cps-state-atom-83 cps-state-atom-85
				   cps-state-iter-yield-86
				   cps-state-atom-87 cps-state-atom-88
				   cps-state-while-84
				   cps-state-atom-89
				   cps-binding---ack---90
				   cps-state-atom-91 cps-state-let*-92
				   cps-state-atom-93 cps-state-atom-95
				   cps-state-iter-yield-96
				   cps-state-atom-97 cps-state-atom-98
				   cps-state-while-94
				   cps-state-atom-99)
	   (setq cps-state-terminal-79
		 #'(lambda nil
		     (signal 'iter-end-of-sequence
			     cps-current-value-77)))
	   (setq cps-state-atom-81
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (let* nil
				 (cond
				  ((eq cps-binding---ack---80 :ok) nil)
				  ((eq cps-binding---ack---80
				       :error-closed)
				   (signal 'nelisp-actor-error
					   (list 'chan-send-on-closed)))
				  (t
				   (signal 'nelisp-actor-error
					   (list 'chan-send-unexpected
						 cps-binding---ack---80)))))
			     (setq cps-current-state-78
				   cps-state-terminal-79)))))
	   (setq cps-state-let*-82
		 #'(lambda nil
		     (progn
		       (setq cps-binding---ack---80
			     cps-current-value-77)
		       (setq cps-current-state-78 cps-state-atom-81))))
	   (setq cps-state-atom-83
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (progn
				 (nelisp-actor--mailbox-pop
				  nelisp-actor--current))
			     (setq cps-current-state-78
				   cps-state-let*-82)))))
	   (setq cps-state-atom-85
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (null
				(nelisp-actor-mailbox
				 nelisp-actor--current))
			     (setq cps-current-state-78
				   cps-state-while-84)))))
	   (setq cps-state-iter-yield-86
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-78 cps-state-atom-85)
		       (throw 'cps--yield cps-current-value-77))))
	   (setq cps-state-atom-87
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1 'nelisp-actor--receive
			     (setq cps-current-state-78
				   cps-state-iter-yield-86)))))
	   (setq cps-state-atom-88
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (let* ((v nelisp-actor--current))
				 (nelisp-actor--set-status v
							   :blocked-receive))
			     (setq cps-current-state-78
				   cps-state-atom-87)))))
	   (setq cps-state-while-84
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-78
			     (if cps-current-value-77
				 cps-state-atom-88
			       cps-state-atom-83)))))
	   (setq cps-state-atom-89
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (nelisp-send (nelisp-chan-actor ch)
					    (list :send :second
						  (nelisp-self)))
			     (setq cps-current-state-78
				   cps-state-atom-85)))))
	   (setq cps-state-atom-91
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (let* nil
				 (cond
				  ((eq cps-binding---ack---90 :ok) nil)
				  ((eq cps-binding---ack---90
				       :error-closed)
				   (signal 'nelisp-actor-error
					   (list 'chan-send-on-closed)))
				  (t
				   (signal 'nelisp-actor-error
					   (list 'chan-send-unexpected
						 cps-binding---ack---90)))))
			     (setq cps-current-state-78
				   cps-state-atom-89)))))
	   (setq cps-state-let*-92
		 #'(lambda nil
		     (progn
		       (setq cps-binding---ack---90
			     cps-current-value-77)
		       (setq cps-current-state-78 cps-state-atom-91))))
	   (setq cps-state-atom-93
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (progn
				 (nelisp-actor--mailbox-pop
				  nelisp-actor--current))
			     (setq cps-current-state-78
				   cps-state-let*-92)))))
	   (setq cps-state-atom-95
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (null
				(nelisp-actor-mailbox
				 nelisp-actor--current))
			     (setq cps-current-state-78
				   cps-state-while-94)))))
	   (setq cps-state-iter-yield-96
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-78 cps-state-atom-95)
		       (throw 'cps--yield cps-current-value-77))))
	   (setq cps-state-atom-97
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1 'nelisp-actor--receive
			     (setq cps-current-state-78
				   cps-state-iter-yield-96)))))
	   (setq cps-state-atom-98
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (let* ((v nelisp-actor--current))
				 (nelisp-actor--set-status v
							   :blocked-receive))
			     (setq cps-current-state-78
				   cps-state-atom-97)))))
	   (setq cps-state-while-94
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-78
			     (if cps-current-value-77
				 cps-state-atom-98
			       cps-state-atom-93)))))
	   (setq cps-state-atom-99
		 #'(lambda nil
		     (setq cps-current-value-77
			   (prog1
			       (nelisp-send (nelisp-chan-actor ch)
					    (list :send :first
						  (nelisp-self)))
			     (setq cps-current-state-78
				   cps-state-atom-95)))))
	   (setq cps-current-state-78 cps-state-atom-99)
	   (let
	       ((iterator
		 #'(lambda (op value)
		     (cond
		      ((eq op :close)
		       (progn
			 (setq cps-current-state-78
			       cps-state-terminal-79)
			 (setq cps-current-value-77 nil)))
		      ((eq op :next) (setq cps-current-value-77 value)
		       (let ((yielded nil))
			 (unwind-protect
			     (prog1
				 (catch 'cps--yield
				   (while t
				     (funcall cps-current-state-78)))
			       (setq yielded t))
			   (if yielded nil
			     (progn
			       (setq cps-current-state-78
				     cps-state-terminal-79)
			       (setq cps-current-value-77 nil))))))
		      (t (error "Unknown iterator operation %S" op))))))
	     nil iterator))))
    (nelisp-actor-run-until-idle) (nreverse seen)))

(defun nl-clj-async-demo-repeated-put-standalone nil
  "Exercise one `nl-clj->!' park point twice from the same `while'.\nThe consumer deliberately uses the raw `nelisp-chan-recv' primitive,\nso this is the put-side counterpart of the minimal repeated-take gate."
  (nelisp-actor--reset)
  (let ((ch (nl-clj-chan)) (accepted nil) (seen nil))
    (nelisp-spawn
     #'(lambda nil
	 (let
	     (cps-current-state-101 cps-current-value-100
				    cps-state-terminal-102
				    cps-binding-i-103
				    cps-state-atom-105
				    cps-state-atom-106
				    cps-binding-ok-107
				    cps-state-atom-108
				    cps-state-let*-109
				    cps-binding-cps-argument-110-111
				    cps-state-atom-112
				    cps-state-let*-113
				    cps-binding-cps-argument-114-115
				    cps-state-atom-116
				    cps-state-let*-117
				    cps-state-atom-118
				    cps-state-atom-120
				    cps-state-iter-yield-121
				    cps-state-atom-122
				    cps-state-atom-123
				    cps-state-while-119
				    cps-state-atom-124
				    cps-state-while-104
				    cps-state-let*-125
				    cps-state-atom-126)
	   (setq cps-state-terminal-102
		 #'(lambda nil
		     (signal 'iter-end-of-sequence
			     cps-current-value-100)))
	   (setq cps-state-atom-105
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1 (< cps-binding-i-103 2)
			     (setq cps-current-state-101
				   cps-state-while-104)))))
	   (setq cps-state-atom-106
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1
			       (progn
				 (setq cps-binding-i-103
				       (1+ cps-binding-i-103)))
			     (setq cps-current-state-101
				   cps-state-atom-105)))))
	   (setq cps-state-atom-108
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1
			       (let* nil
				 (setq accepted
				       (cons cps-binding-ok-107
					     accepted)))
			     (setq cps-current-state-101
				   cps-state-atom-106)))))
	   (setq cps-state-let*-109
		 #'(lambda nil
		     (progn
		       (setq cps-binding-ok-107 cps-current-value-100)
		       (setq cps-current-state-101 cps-state-atom-108))))
	   (setq cps-state-atom-112
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1
			       (let* nil
				 (not cps-binding-cps-argument-110-111))
			     (setq cps-current-state-101
				   cps-state-let*-109)))))
	   (setq cps-state-let*-113
		 #'(lambda nil
		     (progn
		       (setq cps-binding-cps-argument-110-111
			     cps-current-value-100)
		       (setq cps-current-state-101 cps-state-atom-112))))
	   (setq cps-state-atom-116
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1
			       (let* nil
				 (eq cps-binding-cps-argument-114-115
				     :error-closed))
			     (setq cps-current-state-101
				   cps-state-let*-113)))))
	   (setq cps-state-let*-117
		 #'(lambda nil
		     (progn
		       (setq cps-binding-cps-argument-114-115
			     cps-current-value-100)
		       (setq cps-current-state-101 cps-state-atom-116))))
	   (setq cps-state-atom-118
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1
			       (progn
				 (nelisp-actor--mailbox-pop
				  nelisp-actor--current))
			     (setq cps-current-state-101
				   cps-state-let*-117)))))
	   (setq cps-state-atom-120
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1
			       (null
				(nelisp-actor-mailbox
				 nelisp-actor--current))
			     (setq cps-current-state-101
				   cps-state-while-119)))))
	   (setq cps-state-iter-yield-121
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-101 cps-state-atom-120)
		       (throw 'cps--yield cps-current-value-100))))
	   (setq cps-state-atom-122
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1 'nelisp-actor--receive
			     (setq cps-current-state-101
				   cps-state-iter-yield-121)))))
	   (setq cps-state-atom-123
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1
			       (let* ((v nelisp-actor--current))
				 (nelisp-actor--set-status v
							   :blocked-receive))
			     (setq cps-current-state-101
				   cps-state-atom-122)))))
	   (setq cps-state-while-119
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-101
			     (if cps-current-value-100
				 cps-state-atom-123
			       cps-state-atom-118)))))
	   (setq cps-state-atom-124
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1
			       (nelisp-send (nelisp-chan-actor ch)
					    (list :send
						  cps-binding-i-103
						  (nelisp-self)))
			     (setq cps-current-state-101
				   cps-state-atom-120)))))
	   (setq cps-state-while-104
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-101
			     (if cps-current-value-100
				 cps-state-atom-124
			       cps-state-terminal-102)))))
	   (setq cps-state-let*-125
		 #'(lambda nil
		     (progn
		       (setq cps-binding-i-103 cps-current-value-100)
		       (setq cps-current-state-101 cps-state-atom-105))))
	   (setq cps-state-atom-126
		 #'(lambda nil
		     (setq cps-current-value-100
			   (prog1 0
			     (setq cps-current-state-101
				   cps-state-let*-125)))))
	   (setq cps-current-state-101 cps-state-atom-126)
	   (let
	       ((iterator
		 #'(lambda (op value)
		     (cond
		      ((eq op :close)
		       (progn
			 (setq cps-current-state-101
			       cps-state-terminal-102)
			 (setq cps-current-value-100 nil)))
		      ((eq op :next)
		       (setq cps-current-value-100 value)
		       (let ((yielded nil))
			 (unwind-protect
			     (prog1
				 (catch 'cps--yield
				   (while t
				     (funcall cps-current-state-101)))
			       (setq yielded t))
			   (if yielded nil
			     (progn
			       (setq cps-current-state-101
				     cps-state-terminal-102)
			       (setq cps-current-value-100 nil))))))
		      (t (error "Unknown iterator operation %S" op))))))
	     nil iterator))))
    (nelisp-spawn
     #'(lambda nil
	 (let
	     (cps-current-state-128 cps-current-value-127
				    cps-state-terminal-129
				    cps-binding-i-130
				    cps-state-atom-132
				    cps-state-atom-133
				    cps-binding-v-134
				    cps-state-atom-135
				    cps-state-let*-136
				    cps-binding-cps-argument-137-138
				    cps-state-atom-139
				    cps-state-let*-140
				    cps-binding-cps-argument-141-142
				    cps-state-atom-143
				    cps-state-let*-144
				    cps-state-atom-145
				    cps-state-atom-147
				    cps-state-iter-yield-148
				    cps-state-atom-149
				    cps-state-atom-150
				    cps-state-while-146
				    cps-state-atom-151
				    cps-state-while-131
				    cps-state-let*-152
				    cps-state-atom-153)
	   (setq cps-state-terminal-129
		 #'(lambda nil
		     (signal 'iter-end-of-sequence
			     cps-current-value-127)))
	   (setq cps-state-atom-132
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1 (< cps-binding-i-130 2)
			     (setq cps-current-state-128
				   cps-state-while-131)))))
	   (setq cps-state-atom-133
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1
			       (progn
				 (setq cps-binding-i-130
				       (1+ cps-binding-i-130)))
			     (setq cps-current-state-128
				   cps-state-atom-132)))))
	   (setq cps-state-atom-135
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1
			       (let* nil
				 (setq seen
				       (cons cps-binding-v-134 seen)))
			     (setq cps-current-state-128
				   cps-state-atom-133)))))
	   (setq cps-state-let*-136
		 #'(lambda nil
		     (progn
		       (setq cps-binding-v-134 cps-current-value-127)
		       (setq cps-current-state-128 cps-state-atom-135))))
	   (setq cps-state-atom-139
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1
			       (let* nil
				 (car cps-binding-cps-argument-137-138))
			     (setq cps-current-state-128
				   cps-state-let*-136)))))
	   (setq cps-state-let*-140
		 #'(lambda nil
		     (progn
		       (setq cps-binding-cps-argument-137-138
			     cps-current-value-127)
		       (setq cps-current-state-128 cps-state-atom-139))))
	   (setq cps-state-atom-143
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1
			       (let* nil
				 (cdr cps-binding-cps-argument-141-142))
			     (setq cps-current-state-128
				   cps-state-let*-140)))))
	   (setq cps-state-let*-144
		 #'(lambda nil
		     (progn
		       (setq cps-binding-cps-argument-141-142
			     cps-current-value-127)
		       (setq cps-current-state-128 cps-state-atom-143))))
	   (setq cps-state-atom-145
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1
			       (progn
				 (nelisp-actor--mailbox-pop
				  nelisp-actor--current))
			     (setq cps-current-state-128
				   cps-state-let*-144)))))
	   (setq cps-state-atom-147
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1
			       (null
				(nelisp-actor-mailbox
				 nelisp-actor--current))
			     (setq cps-current-state-128
				   cps-state-while-146)))))
	   (setq cps-state-iter-yield-148
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-128 cps-state-atom-147)
		       (throw 'cps--yield cps-current-value-127))))
	   (setq cps-state-atom-149
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1 'nelisp-actor--receive
			     (setq cps-current-state-128
				   cps-state-iter-yield-148)))))
	   (setq cps-state-atom-150
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1
			       (let* ((v nelisp-actor--current))
				 (nelisp-actor--set-status v
							   :blocked-receive))
			     (setq cps-current-state-128
				   cps-state-atom-149)))))
	   (setq cps-state-while-146
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-128
			     (if cps-current-value-127
				 cps-state-atom-150
			       cps-state-atom-145)))))
	   (setq cps-state-atom-151
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1
			       (nelisp-send (nelisp-chan-actor ch)
					    (list :recv (nelisp-self)))
			     (setq cps-current-state-128
				   cps-state-atom-147)))))
	   (setq cps-state-while-131
		 #'(lambda nil
		     (progn
		       (setq cps-current-state-128
			     (if cps-current-value-127
				 cps-state-atom-151
			       cps-state-terminal-129)))))
	   (setq cps-state-let*-152
		 #'(lambda nil
		     (progn
		       (setq cps-binding-i-130 cps-current-value-127)
		       (setq cps-current-state-128 cps-state-atom-132))))
	   (setq cps-state-atom-153
		 #'(lambda nil
		     (setq cps-current-value-127
			   (prog1 0
			     (setq cps-current-state-128
				   cps-state-let*-152)))))
	   (setq cps-current-state-128 cps-state-atom-153)
	   (let
	       ((iterator
		 #'(lambda (op value)
		     (cond
		      ((eq op :close)
		       (progn
			 (setq cps-current-state-128
			       cps-state-terminal-129)
			 (setq cps-current-value-127 nil)))
		      ((eq op :next)
		       (setq cps-current-value-127 value)
		       (let ((yielded nil))
			 (unwind-protect
			     (prog1
				 (catch 'cps--yield
				   (while t
				     (funcall cps-current-state-128)))
			       (setq yielded t))
			   (if yielded nil
			     (progn
			       (setq cps-current-state-128
				     cps-state-terminal-129)
			       (setq cps-current-value-127 nil))))))
		      (t (error "Unknown iterator operation %S" op))))))
	     nil iterator))))
    (nelisp-actor-run-until-idle)
    (list :accepted (nreverse accepted) :seen (nreverse seen))))

(defun nl-clj-async-demo-ping-pong-standalone (hops)
  "Bounce a counter between two `nl-clj-go' blocks, each talking over\nits own channel, for HOPS round trips.  HOPS must be >= 1 -- ping's\nown initial rendezvous send requires pong to execute at least one\nreceive/reply cycle.  Returns a plist:\n\n  :trail        ((ACTOR . N) ...) in the order each hop was received --\n                same shape as `nelisp-demo-ping-pong''s own :trail\n  :ping-result  the ping go-block's own return value (`:ping-done')\n  :pong-result  likewise for pong (`:pong-done')\n\nBoth sides terminate cleanly (unlike the raw-actor demo this mirrors,\nwhich deliberately leaves ping dangling for every HOPS value): pong\nalways replies exactly HOPS times; ping sends the initial value plus\nexactly (HOPS - 1) replies, so the last exchange is pong's alone --\nsymmetric, counter-bounded termination on both sides, no `close!'\nneeded to unpark a final dangling receive."
  (nelisp-actor--reset)
  (let
      ((to-ping (nl-clj-chan)) (to-pong (nl-clj-chan)) (trail nil)
       pong-chan ping-chan)
    (setq pong-chan
	  (let ((nl-clj-go-chan154 (nl-clj-chan 1)))
	    (nelisp-spawn
	     #'(lambda nil
		 (let
		     (cps-current-state-156 cps-current-value-155
					    cps-state-terminal-157
					    cps-state-atom-158
					    cps-binding---ack---159
					    cps-state-atom-160
					    cps-state-let*-161
					    cps-state-atom-162
					    cps-state-atom-164
					    cps-state-iter-yield-165
					    cps-state-atom-166
					    cps-state-atom-167
					    cps-state-while-163
					    cps-state-atom-168
					    cps-binding-i-169
					    cps-state-atom-171
					    cps-state-atom-172
					    cps-binding-n-173
					    cps-binding-cps-argument-174-175
					    cps-state-atom-176
					    cps-state-let*-177
					    cps-binding-cps-argument-178-179
					    cps-state-atom-180
					    cps-state-let*-181
					    cps-state-atom-182
					    cps-state-atom-184
					    cps-state-iter-yield-185
					    cps-state-atom-186
					    cps-state-atom-187
					    cps-state-while-183
					    cps-state-atom-188
					    cps-state-atom-189
					    cps-state-let*-190
					    cps-binding-cps-argument-191-192
					    cps-state-atom-193
					    cps-state-let*-194
					    cps-state-atom-195
					    cps-state-atom-197
					    cps-state-iter-yield-198
					    cps-state-atom-199
					    cps-state-atom-200
					    cps-state-while-196
					    cps-state-atom-201
					    cps-state-while-170
					    cps-state-let*-202
					    cps-state-atom-203)
		   (setq cps-state-terminal-157
			 #'(lambda nil
			     (signal 'iter-end-of-sequence
				     cps-current-value-155)))
		   (setq cps-state-atom-158
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (progn
					 (nelisp-chan-close
					  nl-clj-go-chan154))
				     (setq cps-current-state-156
					   cps-state-terminal-157)))))
		   (setq cps-state-atom-160
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (let* nil
					 (cond
					  ((eq cps-binding---ack---159
					       :ok)
					   nil)
					  ((eq cps-binding---ack---159
					       :error-closed)
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-on-closed)))
					  (t
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-unexpected
						    cps-binding---ack---159)))))
				     (setq cps-current-state-156
					   cps-state-atom-158)))))
		   (setq cps-state-let*-161
			 #'(lambda nil
			     (progn
			       (setq cps-binding---ack---159
				     cps-current-value-155)
			       (setq cps-current-state-156
				     cps-state-atom-160))))
		   (setq cps-state-atom-162
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-156
					   cps-state-let*-161)))))
		   (setq cps-state-atom-164
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-156
					   cps-state-while-163)))))
		   (setq cps-state-iter-yield-165
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-156
				     cps-state-atom-164)
			       (throw 'cps--yield
				      cps-current-value-155))))
		   (setq cps-state-atom-166
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-156
					   cps-state-iter-yield-165)))))
		   (setq cps-state-atom-167
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-156
					   cps-state-atom-166)))))
		   (setq cps-state-while-163
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-156
				     (if cps-current-value-155
					 cps-state-atom-167
				       cps-state-atom-162)))))
		   (setq cps-state-atom-168
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor
					 nl-clj-go-chan154)
					(list :send :pong-done
					      (nelisp-self)))
				     (setq cps-current-state-156
					   cps-state-atom-164)))))
		   (setq cps-state-atom-171
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1 (< cps-binding-i-169 hops)
				     (setq cps-current-state-156
					   cps-state-while-170)))))
		   (setq cps-state-atom-172
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (progn
					 (setq cps-binding-i-169
					       (1+ cps-binding-i-169)))
				     (setq cps-current-state-156
					   cps-state-atom-171)))))
		   (setq cps-state-atom-176
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (let* nil
					 (not
					  cps-binding-cps-argument-174-175))
				     (setq cps-current-state-156
					   cps-state-atom-172)))))
		   (setq cps-state-let*-177
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-174-175
				     cps-current-value-155)
			       (setq cps-current-state-156
				     cps-state-atom-176))))
		   (setq cps-state-atom-180
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (let* nil
					 (eq
					  cps-binding-cps-argument-178-179
					  :error-closed))
				     (setq cps-current-state-156
					   cps-state-let*-177)))))
		   (setq cps-state-let*-181
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-178-179
				     cps-current-value-155)
			       (setq cps-current-state-156
				     cps-state-atom-180))))
		   (setq cps-state-atom-182
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-156
					   cps-state-let*-181)))))
		   (setq cps-state-atom-184
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-156
					   cps-state-while-183)))))
		   (setq cps-state-iter-yield-185
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-156
				     cps-state-atom-184)
			       (throw 'cps--yield
				      cps-current-value-155))))
		   (setq cps-state-atom-186
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-156
					   cps-state-iter-yield-185)))))
		   (setq cps-state-atom-187
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-156
					   cps-state-atom-186)))))
		   (setq cps-state-while-183
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-156
				     (if cps-current-value-155
					 cps-state-atom-187
				       cps-state-atom-182)))))
		   (setq cps-state-atom-188
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-ping)
					(list :send
					      (1+ cps-binding-n-173)
					      (nelisp-self)))
				     (setq cps-current-state-156
					   cps-state-atom-184)))))
		   (setq cps-state-atom-189
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (setq trail
					     (cons
					      (cons 'pong
						    cps-binding-n-173)
					      trail))
				     (setq cps-current-state-156
					   cps-state-atom-188)))))
		   (setq cps-state-let*-190
			 #'(lambda nil
			     (progn
			       (setq cps-binding-n-173
				     cps-current-value-155)
			       (setq cps-current-state-156
				     cps-state-atom-189))))
		   (setq cps-state-atom-193
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (let* nil
					 (nl-clj-async--unwrap-recv
					  cps-binding-cps-argument-191-192))
				     (setq cps-current-state-156
					   cps-state-let*-190)))))
		   (setq cps-state-let*-194
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-191-192
				     cps-current-value-155)
			       (setq cps-current-state-156
				     cps-state-atom-193))))
		   (setq cps-state-atom-195
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-156
					   cps-state-let*-194)))))
		   (setq cps-state-atom-197
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-156
					   cps-state-while-196)))))
		   (setq cps-state-iter-yield-198
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-156
				     cps-state-atom-197)
			       (throw 'cps--yield
				      cps-current-value-155))))
		   (setq cps-state-atom-199
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-156
					   cps-state-iter-yield-198)))))
		   (setq cps-state-atom-200
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-156
					   cps-state-atom-199)))))
		   (setq cps-state-while-196
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-156
				     (if cps-current-value-155
					 cps-state-atom-200
				       cps-state-atom-195)))))
		   (setq cps-state-atom-201
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-pong)
					(list :recv (nelisp-self)))
				     (setq cps-current-state-156
					   cps-state-atom-197)))))
		   (setq cps-state-while-170
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-156
				     (if cps-current-value-155
					 cps-state-atom-201
				       cps-state-atom-168)))))
		   (setq cps-state-let*-202
			 #'(lambda nil
			     (progn
			       (setq cps-binding-i-169
				     cps-current-value-155)
			       (setq cps-current-state-156
				     cps-state-atom-171))))
		   (setq cps-state-atom-203
			 #'(lambda nil
			     (setq cps-current-value-155
				   (prog1 0
				     (setq cps-current-state-156
					   cps-state-let*-202)))))
		   (setq cps-current-state-156 cps-state-atom-203)
		   (let
		       ((iterator
			 #'(lambda (op value)
			     (cond
			      ((eq op :close)
			       (progn
				 (setq cps-current-state-156
				       cps-state-terminal-157)
				 (setq cps-current-value-155 nil)))
			      ((eq op :next)
			       (setq cps-current-value-155 value)
			       (let ((yielded nil))
				 (unwind-protect
				     (prog1
					 (catch 'cps--yield
					   (while t
					     (funcall
					      cps-current-state-156)))
				       (setq yielded t))
				   (if yielded nil
				     (progn
				       (setq cps-current-state-156
					     cps-state-terminal-157)
				       (setq cps-current-value-155 nil))))))
			      (t
			       (error "Unknown iterator operation %S"
				      op))))))
		     nil iterator))))
	    nl-clj-go-chan154))
    (setq ping-chan
	  (let ((nl-clj-go-chan204 (nl-clj-chan 1)))
	    (nelisp-spawn
	     #'(lambda nil
		 (let
		     (cps-current-state-206 cps-current-value-205
					    cps-state-terminal-207
					    cps-state-atom-208
					    cps-binding---ack---209
					    cps-state-atom-210
					    cps-state-let*-211
					    cps-state-atom-212
					    cps-state-atom-214
					    cps-state-iter-yield-215
					    cps-state-atom-216
					    cps-state-atom-217
					    cps-state-while-213
					    cps-state-atom-218
					    cps-binding-i-219
					    cps-state-atom-221
					    cps-state-atom-222
					    cps-binding-n-223
					    cps-binding-cps-argument-224-225
					    cps-state-atom-226
					    cps-state-let*-227
					    cps-binding-cps-argument-228-229
					    cps-state-atom-230
					    cps-state-let*-231
					    cps-state-atom-232
					    cps-state-atom-234
					    cps-state-iter-yield-235
					    cps-state-atom-236
					    cps-state-atom-237
					    cps-state-while-233
					    cps-state-atom-238
					    cps-state-atom-239
					    cps-state-if-240
					    cps-state-atom-241
					    cps-state-atom-242
					    cps-state-let*-243
					    cps-binding-cps-argument-244-245
					    cps-state-atom-246
					    cps-state-let*-247
					    cps-state-atom-248
					    cps-state-atom-250
					    cps-state-iter-yield-251
					    cps-state-atom-252
					    cps-state-atom-253
					    cps-state-while-249
					    cps-state-atom-254
					    cps-state-while-220
					    cps-state-let*-255
					    cps-state-atom-256
					    cps-binding-cps-argument-257-258
					    cps-state-atom-259
					    cps-state-let*-260
					    cps-binding-cps-argument-261-262
					    cps-state-atom-263
					    cps-state-let*-264
					    cps-state-atom-265
					    cps-state-atom-267
					    cps-state-iter-yield-268
					    cps-state-atom-269
					    cps-state-atom-270
					    cps-state-while-266
					    cps-state-atom-271)
		   (setq cps-state-terminal-207
			 #'(lambda nil
			     (signal 'iter-end-of-sequence
				     cps-current-value-205)))
		   (setq cps-state-atom-208
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (progn
					 (nelisp-chan-close
					  nl-clj-go-chan204))
				     (setq cps-current-state-206
					   cps-state-terminal-207)))))
		   (setq cps-state-atom-210
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let* nil
					 (cond
					  ((eq cps-binding---ack---209
					       :ok)
					   nil)
					  ((eq cps-binding---ack---209
					       :error-closed)
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-on-closed)))
					  (t
					   (signal 'nelisp-actor-error
						   (list
						    'chan-send-unexpected
						    cps-binding---ack---209)))))
				     (setq cps-current-state-206
					   cps-state-atom-208)))))
		   (setq cps-state-let*-211
			 #'(lambda nil
			     (progn
			       (setq cps-binding---ack---209
				     cps-current-value-205)
			       (setq cps-current-state-206
				     cps-state-atom-210))))
		   (setq cps-state-atom-212
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-206
					   cps-state-let*-211)))))
		   (setq cps-state-atom-214
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-206
					   cps-state-while-213)))))
		   (setq cps-state-iter-yield-215
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-206
				     cps-state-atom-214)
			       (throw 'cps--yield
				      cps-current-value-205))))
		   (setq cps-state-atom-216
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-206
					   cps-state-iter-yield-215)))))
		   (setq cps-state-atom-217
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-206
					   cps-state-atom-216)))))
		   (setq cps-state-while-213
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-206
				     (if cps-current-value-205
					 cps-state-atom-217
				       cps-state-atom-212)))))
		   (setq cps-state-atom-218
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor
					 nl-clj-go-chan204)
					(list :send :ping-done
					      (nelisp-self)))
				     (setq cps-current-state-206
					   cps-state-atom-214)))))
		   (setq cps-state-atom-221
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1 (< cps-binding-i-219 hops)
				     (setq cps-current-state-206
					   cps-state-while-220)))))
		   (setq cps-state-atom-222
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (progn
					 (setq cps-binding-i-219
					       (1+ cps-binding-i-219)))
				     (setq cps-current-state-206
					   cps-state-atom-221)))))
		   (setq cps-state-atom-226
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let* nil
					 (not
					  cps-binding-cps-argument-224-225))
				     (setq cps-current-state-206
					   cps-state-atom-222)))))
		   (setq cps-state-let*-227
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-224-225
				     cps-current-value-205)
			       (setq cps-current-state-206
				     cps-state-atom-226))))
		   (setq cps-state-atom-230
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let* nil
					 (eq
					  cps-binding-cps-argument-228-229
					  :error-closed))
				     (setq cps-current-state-206
					   cps-state-let*-227)))))
		   (setq cps-state-let*-231
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-228-229
				     cps-current-value-205)
			       (setq cps-current-state-206
				     cps-state-atom-230))))
		   (setq cps-state-atom-232
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-206
					   cps-state-let*-231)))))
		   (setq cps-state-atom-234
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-206
					   cps-state-while-233)))))
		   (setq cps-state-iter-yield-235
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-206
				     cps-state-atom-234)
			       (throw 'cps--yield
				      cps-current-value-205))))
		   (setq cps-state-atom-236
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-206
					   cps-state-iter-yield-235)))))
		   (setq cps-state-atom-237
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-206
					   cps-state-atom-236)))))
		   (setq cps-state-while-233
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-206
				     (if cps-current-value-205
					 cps-state-atom-237
				       cps-state-atom-232)))))
		   (setq cps-state-atom-238
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-pong)
					(list :send
					      (1+ cps-binding-n-223)
					      (nelisp-self)))
				     (setq cps-current-state-206
					   cps-state-atom-234)))))
		   (setq cps-state-atom-239
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1 (progn)
				     (setq cps-current-state-206
					   cps-state-atom-222)))))
		   (setq cps-state-if-240
			 #'(lambda nil
			     (setq cps-current-state-206
				   (if cps-current-value-205
				       cps-state-atom-238
				     cps-state-atom-239))))
		   (setq cps-state-atom-241
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (< (1+ cps-binding-i-219) hops)
				     (setq cps-current-state-206
					   cps-state-if-240)))))
		   (setq cps-state-atom-242
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (setq trail
					     (cons
					      (cons 'ping
						    cps-binding-n-223)
					      trail))
				     (setq cps-current-state-206
					   cps-state-atom-241)))))
		   (setq cps-state-let*-243
			 #'(lambda nil
			     (progn
			       (setq cps-binding-n-223
				     cps-current-value-205)
			       (setq cps-current-state-206
				     cps-state-atom-242))))
		   (setq cps-state-atom-246
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let* nil
					 (nl-clj-async--unwrap-recv
					  cps-binding-cps-argument-244-245))
				     (setq cps-current-state-206
					   cps-state-let*-243)))))
		   (setq cps-state-let*-247
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-244-245
				     cps-current-value-205)
			       (setq cps-current-state-206
				     cps-state-atom-246))))
		   (setq cps-state-atom-248
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-206
					   cps-state-let*-247)))))
		   (setq cps-state-atom-250
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-206
					   cps-state-while-249)))))
		   (setq cps-state-iter-yield-251
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-206
				     cps-state-atom-250)
			       (throw 'cps--yield
				      cps-current-value-205))))
		   (setq cps-state-atom-252
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-206
					   cps-state-iter-yield-251)))))
		   (setq cps-state-atom-253
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-206
					   cps-state-atom-252)))))
		   (setq cps-state-while-249
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-206
				     (if cps-current-value-205
					 cps-state-atom-253
				       cps-state-atom-248)))))
		   (setq cps-state-atom-254
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-ping)
					(list :recv (nelisp-self)))
				     (setq cps-current-state-206
					   cps-state-atom-250)))))
		   (setq cps-state-while-220
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-206
				     (if cps-current-value-205
					 cps-state-atom-254
				       cps-state-atom-218)))))
		   (setq cps-state-let*-255
			 #'(lambda nil
			     (progn
			       (setq cps-binding-i-219
				     cps-current-value-205)
			       (setq cps-current-state-206
				     cps-state-atom-221))))
		   (setq cps-state-atom-256
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1 0
				     (setq cps-current-state-206
					   cps-state-let*-255)))))
		   (setq cps-state-atom-259
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let* nil
					 (not
					  cps-binding-cps-argument-257-258))
				     (setq cps-current-state-206
					   cps-state-atom-256)))))
		   (setq cps-state-let*-260
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-257-258
				     cps-current-value-205)
			       (setq cps-current-state-206
				     cps-state-atom-259))))
		   (setq cps-state-atom-263
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let* nil
					 (eq
					  cps-binding-cps-argument-261-262
					  :error-closed))
				     (setq cps-current-state-206
					   cps-state-let*-260)))))
		   (setq cps-state-let*-264
			 #'(lambda nil
			     (progn
			       (setq cps-binding-cps-argument-261-262
				     cps-current-value-205)
			       (setq cps-current-state-206
				     cps-state-atom-263))))
		   (setq cps-state-atom-265
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (progn
					 (nelisp-actor--mailbox-pop
					  nelisp-actor--current))
				     (setq cps-current-state-206
					   cps-state-let*-264)))))
		   (setq cps-state-atom-267
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (null
					(nelisp-actor-mailbox
					 nelisp-actor--current))
				     (setq cps-current-state-206
					   cps-state-while-266)))))
		   (setq cps-state-iter-yield-268
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-206
				     cps-state-atom-267)
			       (throw 'cps--yield
				      cps-current-value-205))))
		   (setq cps-state-atom-269
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1 'nelisp-actor--receive
				     (setq cps-current-state-206
					   cps-state-iter-yield-268)))))
		   (setq cps-state-atom-270
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (let*
					   ((v nelisp-actor--current))
					 (nelisp-actor--set-status v
								   :blocked-receive))
				     (setq cps-current-state-206
					   cps-state-atom-269)))))
		   (setq cps-state-while-266
			 #'(lambda nil
			     (progn
			       (setq cps-current-state-206
				     (if cps-current-value-205
					 cps-state-atom-270
				       cps-state-atom-265)))))
		   (setq cps-state-atom-271
			 #'(lambda nil
			     (setq cps-current-value-205
				   (prog1
				       (nelisp-send
					(nelisp-chan-actor to-pong)
					(list :send 0 (nelisp-self)))
				     (setq cps-current-state-206
					   cps-state-atom-267)))))
		   (setq cps-current-state-206 cps-state-atom-271)
		   (let
		       ((iterator
			 #'(lambda (op value)
			     (cond
			      ((eq op :close)
			       (progn
				 (setq cps-current-state-206
				       cps-state-terminal-207)
				 (setq cps-current-value-205 nil)))
			      ((eq op :next)
			       (setq cps-current-value-205 value)
			       (let ((yielded nil))
				 (unwind-protect
				     (prog1
					 (catch 'cps--yield
					   (while t
					     (funcall
					      cps-current-state-206)))
				       (setq yielded t))
				   (if yielded nil
				     (progn
				       (setq cps-current-state-206
					     cps-state-terminal-207)
				       (setq cps-current-value-205 nil))))))
			      (t
			       (error "Unknown iterator operation %S"
				      op))))))
		     nil iterator))))
	    nl-clj-go-chan204))
    (nelisp-actor-run-until-idle)
    (list :trail (nreverse trail) :ping-result (nl-clj-<!! ping-chan)
	  :pong-result (nl-clj-<!! pong-chan))))

(setq nl-clj-async--chan-ctor-standalone
      #'nl-clj-async--make-chan-1-standalone)
(setq nl-clj-async--blocking-take-standalone
      #'nl-clj-async--blocking-take-1-standalone)
(setq nl-clj-async--blocking-put-standalone
      #'nl-clj-async--blocking-put-1-standalone)

(provide 'go-ping-pong-cps)

;;; go-ping-pong-cps.el ends here
