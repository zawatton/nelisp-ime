#+title: NeLisp Phase 5-B — Emacs core substrate (buffer / redisplay / event-loop)
#+author: zawatton
#+date: 2026-04-24
#+options: toc:2

* STATE: SHIPPED 2026-04-24 (全 §3 sub-phase SHIPPED + 689/689 green + §2.9 C 達成、merge gate 通過、Phase 5-B 完了)

** 目的

release bar 後半 ("Emacs core 置換") を達成するため、Emacs C で
実装されている 3 核心 (buffer / redisplay / event-loop) の NeLisp
実装を新構築する。*既存 C モジュールを置き換え*、NeLisp 上で
最小限の "editor" が動作することで、NeLisp が Emacs の母体として
働ける substrate であることを実証する。

release bar 前半 (Phase 5-A Doc 12 SHIPPED-2026-04-24) で
"NeLisp 単独でパッケージ作成自立" は達成済。Phase 5-B 完了で
"NeLisp 単独で Emacs 代替 substrate 動作" を目指す。

** 背景

Phase 0 で lock された v1.0 完遂条件 4 項:
1. self-host 到達 — Phase 2 達成 (2026-04-23)
2. *3 大 core module (buffer / redisplay / event-loop) Elisp 化*
3. AI-critical path 5 領域が NeLisp 上で動作
4. 残存 C は SBCL-style 4 核 (syscall / allocator / GC inner / coding)

本 Phase 5-B は項目 2 に対応。anvil.el 開発中に感じた host C
由来の限界 (file-notify freeze / mutex cross-session 不能 /
subprocess 迂回 / Emacs server filter starvation 等) を、buffer
/ redisplay / event-loop の 3 核を Lisp で新設することで
structural に解消する下地を作る。

Rhodes 2008 "SBCL: A Sanely-Bootstrappable Common Lisp" 方針
(Phase 0 decision Bootstrap A) と並走: host C を SBCL 4 核
(syscall / allocator / GC inner / coding) のみに絞り、それ以外
は NeLisp 上に置く。

** Non-goals

- GUI redisplay (X / Windows / Cocoa) は本 Phase 範囲外。
  terminal-only から着手、GUI は Phase 6+ で検討。
- Major / minor mode 実装互換は本 Phase 範囲外。substrate が
  揃えば mode は Elisp で書ける (Phase 5-A の loader で load 可)。
- Unicode normalization の完全対応は範囲外 (encode/decode は
  host に委譲、bytes→chars 変換は Phase 7+)。
- Network I/O substrate (process / network connection) は
  Phase 5-C 以降。本 Phase は端末入出力と buffer I/O のみ。
- =redisplay= の bidi / ligature / font shaping 等は範囲外。
  ASCII + CJK full-width 2 分類の monospace grid のみ扱う。
- =package.el= 互換 installer は Phase 5-A と同じく別課題。

* §1 Phase 5-A 成果前提

本 Phase は Phase 5-A の成果物に依存する:
- =nelisp-load-file= / =nelisp-load= / =nelisp-require= 動作
- =nelisp-load-path= による host 非依存 module 解決
- =nelisp-macroexpand*= 系が primitive dispatch 経由で public
- Phase 2 self-host fixpoint (cycle-1 = cycle-2)
- Phase 3c mark-sweep GC (buffer / overlay の heap 管理)
- Phase 4 actor + channel + supervisor (event loop の下地)

Phase 5-B は新規 module 3 本を =src/nelisp-buffer.el= /
=nelisp-emacs= 兄弟リポジトリの =src/nelisp-redisplay.el= /
=src/nelisp-eventloop.el= として
追加、=test/nelisp-buffer-test.el= 他で ERT 整備。primitive 表
には buffer-related 最小 set (=generate-new-buffer=, =point=,
=goto-char= 等) を Phase 5-B.0 で追加する。

* §2 Locked decisions (signoff 必須)

以下 9 項目を user signoff 後に LOCK する。Doc 10 / Doc 12 と
同じ pattern。default 推奨を示すが、どの項目も差し戻し可能。

** §2.1 Buffer data structure

*Q*: buffer のテキスト表現をどう持つか。Emacs C は gap buffer
(2-part contiguous array + gap region) を使う。

選択肢:
A. Gap buffer (Emacs 同型) — 単純、挿入/削除 O(N/W)、Emacs
   semantics 完全再現しやすい
B. Rope (平衡二分木) — O(log N) 全操作、実装複雑、marker
   整合も再設計要
C. Piece table (VS Code 他) — undo-friendly、実装は比較的
   簡潔、marker は位置リスト保持

*推奨: A (Gap buffer)*。Emacs semantics 再現優先、perf は後続
Phase で移行可。rope/piece-table 移行は data-abstraction を
interface で切る前提で将来 swap 可能に書く。

** §2.2 Text representation

*Q*: バッファ内の文字単位。

選択肢:
A. Host Elisp =string= (multibyte 含む) — Emacs 同等、encode
   / decode は host 委譲
B. Byte vector + 明示 codec layer — 純粋だが encode/decode を
   NeLisp 内実装必要
C. Code-point vector (fix32 配列) — unicode 直球、memory 重い

*推奨: A (string)*。Phase 5-B は correctness が先、codec は
SBCL 4 核に残して host 委譲のままで良い。

** §2.3 Marker / overlay 実装

*Q*: =marker= / =overlay= をどう保持するか。

選択肢:
A. buffer-local doubly-linked list (weak-ref ではなく GC root) —
   Phase 3c mark-sweep が reach 可能、insert/delete で毎 marker
   offset 更新
B. Interval tree — 多 overlay 時に O(log N) query、実装複雑
C. B-tree (Emacs 流、itree) — Emacs 2024+ 方針に準拠

*推奨: A (linked list)*。MVP は overlay 数 < 1000 想定、
linked list で十分。interval tree は perf profile 後移行可。

** §2.4 Redisplay 戦略

*Q*: screen 描画をどう組むか。

選択肢:
A. Terminal-only + buffer 変更毎 full-repaint — 最単純、
   flicker あり
B. Terminal-only + dirty-region incremental (row 単位) —
   flicker 無し、実装中規模
C. Terminal + GUI 両対応 — scope 過大

*推奨: B (terminal dirty-region)*。A は UX 悪すぎ、C は本
Phase 範囲外。window を row x col grid に抽象、modified row
だけ terminal escape で再描画。

** §2.5 Event loop モデル

*Q*: input event ループの基盤。

選択肢:
A. Standalone event loop (new recv loop) — 独立実装、Phase 4
   actor は使わない
B. Phase 4 actor を再利用 — input source を 1 actor として
   spawn、buffer update / redisplay も actor message で trigger
C. Hybrid — keyboard は actor、timer は host

*推奨: B (Phase 4 actor)*。anvil.el pain の多くが event loop
blocking に起因、actor の yield point を活用。Phase 4 の
=nelisp-actor-tick-interval= をそのまま event drain 周期に使う。

** §2.6 Host Emacs との共存戦略

*Q*: NeLisp buffer が host Emacs buffer と同居するとき、どう
名前空間分離するか。

選択肢:
A. Parallel — NeLisp buffer は =nelisp-buffer-*= API、host
   buffer は触らない (Phase 5-B 内は完全隔離)
B. Shim — NeLisp buffer ops は host buffer を wrap
   (bootstrap、perf 同等)
C. Replacement — host buffer 概念を NeLisp-buffer で完全置換
   (Phase 5-B 終点の理想形)

*推奨: A (Parallel)*。NeLisp-buffer の correctness を独立に
検証可、host Emacs の安定性を損なわない。C は Phase 5-C 以降、
Emacs substitute として起動する境地まで行ってから。

** §2.7 Concurrency モデル

*Q*: buffer に対する並行アクセスをどう制御するか。

選択肢:
A. Single-threaded (Emacs 流) — 1 event loop、buffer 操作
   atomic
B. Per-buffer actor (Phase 4) — buffer 毎 1 actor、message
   駆動
C. STM (Phase 4b optional) — log-replay 方式、複雑

*推奨: A (single-threaded)*。Phase 5-B の目的は core semantics
再現、concurrency の上積みは Phase 6+。ただし event loop は
actor で組むので「buffer 操作 = actor message」の自然な延長で
B にいつでも migrate 可能なように interface を切る。

** §2.8 Test strategy

*Q*: NeLisp-buffer の正しさをどう担保するか。

選択肢:
A. ERT unit (insert/delete/point/mark 単体) のみ
B. A + host buffer parity test (同操作で NeLisp-buffer と
   host-buffer の text/point/mark が一致)
C. A + B + round-trip (serialize → restore 一致)

*推奨: B (A + parity)*。Phase 5-A と同じく ~50 ERT 目標、
parity test で semantics 漏れを早期検出。serialize は Phase
5-C で。

** §2.9 Merge gate 成功条件 (§3.6 equivalent)

選択肢:
A. 全 §3 sub-phase SHIPPED + make test green + NeLisp-buffer
   上で =insert= / =delete-region= / =goto-char= 動作
B. A + event loop から key input → buffer update が動く
C. B + redisplay を terminal で visible 描画

*推奨: C*。Phase 5-B の存在理由は "3 核連動で editor 最小
動作"。C 未達では release bar 後半を証明したとは言い難い。
ただし "editor" の scope は Phase 5-B.5 demo で =hello-world=
package を load して visible 表示できるところまで、実用 editor
ではない。

* §3 Decomposition (signoff 後着手)

Phase 4 / 5-A pattern 踏襲: 小さい SHIPPED 刻みで各 sub-phase
を =feat/phase5b.N-NAME= branch に閉じ、=--no-ff= merge で main
に積む。各 branch で make test green が必須。

** §3.0 Pre-req cleanup — SHIPPED 2026-04-24

目的: Phase 5-A の primitive 表 + macro facility で
Phase 5-B 着手に必要な gap を埋める。

- [X] =docs/design/13-audit-script.el= で forecast 4 sketch
  (=13-forecast/nelisp-{buffer,marker,redisplay,eventloop}-sketch.el=)
  を scan、gap list を =13-phase5b-primitive-gap.data.el= へ
- [X] audit 結果: 実 gap 10 primitive (他 2 は struct field 名
  =point= / =mark= が function position と誤認された false
  positive で無視)
- [X] =src/nelisp-eval.el= =nelisp--primitive-symbols= に
  10 primitive 追加:
  - General sequence: =copy-sequence= / =elt= / =nconc= / =delq=
  - String search / split: =string-search= / =split-string=
  - Terminal & frame: =send-string-to-terminal= / =frame-width=
    / =frame-height=
  - Timer scheduling: =run-at-time=
- [X] =test/nelisp-stdlib-test.el= に ERT 11 本追加:
  - =copy-sequence-string= / =copy-sequence-list= (eq 分離確認)
  - =elt-string-and-list= (polymorphic)
  - =nconc-merges-lists= / =delq-removes-eq=
  - =string-search-matches= / =split-string-default= /
    =split-string-with-separator=
  - =frame-width-height-return-positive=
  - =send-string-to-terminal-returns-nil= (registration 確認、
    batch tty 出力の assertion は platform 差で避ける)
  - =run-at-time-registered= (timer 返却 + 即 cancel-timer で
    leak 回避)
- [X] 非追加の false positive 2 件 (=point= / =mark=): cl-defstruct
  の field 名が audit script walker に function position として
  拾われたもの。host buffer primitive は §2.6 A (Parallel) の
  方針通り不追加、nelisp-buffer は自前 =nelisp-buffer-point= /
  =nelisp-buffer-mark= を持つ
- [X] make test: 569 → *580/580 green* (+11, 0 unexpected)
- commit: =e966a91= on =feat/phase5b.0-pre-req=

** §3.1 =nelisp-buffer= primitive (gap buffer + insert/delete) — SHIPPED 2026-04-24

目的: buffer 本体 struct + 最小 text 操作 API。

- [X] =src/nelisp-buffer.el= 新設 (~230 LOC):
  - =cl-defstruct nelisp-buffer= (name / before-gap / after-gap /
    modified / markers / overlays / narrow-start / narrow-end)
  - registry: =nelisp-buffer--registry= (hash-table、name→buf)
  - 現在 buffer: =nelisp-buffer--current= + =nelisp-with-buffer=
    macro で動的束縛
  - 作成: =nelisp-generate-new-buffer= (uniquify =<N>= 付与) /
    =nelisp-get-buffer-create= / =nelisp-get-buffer= /
    =nelisp-kill-buffer= / =nelisp-buffer-list=
  - 位置: =nelisp-point= / =nelisp-point-min= /
    =nelisp-point-max= / =nelisp-buffer-size=
  - テキスト: =nelisp-buffer-string= / =nelisp-buffer-substring=
    / =nelisp-char-after=
  - 編集: =nelisp-insert= / =nelisp-delete-region= /
    =nelisp-goto-char= / =nelisp-erase-buffer=
  - 状態: =nelisp-buffer-modified-p= /
    =nelisp-buffer-set-modified=
  - narrowing: =nelisp-narrow-to-region= / =nelisp-widen= /
    =nelisp-save-restriction= macro / =nelisp-save-excursion=
    macro
  - marker shift helpers (§3.2 で marker struct 化される時まで
    素の =(POS . _)= cons cell を受け付ける前方互換)
- [X] =test/nelisp-buffer-test.el= 新設 (29 ERT):
  - 5 registry (uniquify / get-or-create idempotent / absent /
    kill / list)
  - 5 insert / size / point (empty / basic / multi / middle /
    type-error)
  - 2 goto-char (clamp / narrow 尊重)
  - 4 delete-region (basic / across-gap / inverted /
    out-of-range)
  - 2 substring + char-after
  - 2 erase / modified flag
  - 3 narrow / save-restriction / save-excursion
  - 1 =nelisp-with-buffer= 入れ子
  - 5 host parity (insert-sequence / goto+delete / narrow-bounds
    / buffer-substring / multibyte-Japanese)
- [X] primitive 表は §2.6 A Parallel により追加なし (NeLisp
  源 code は自前 =nelisp-buffer-*= API、host buffer は触らない)
- [X] make test: 580 → *609/609 green* (+29, 0 unexpected)
- commit: =3b59c30= on =feat/phase5b.1-buffer=

期待スコープ 1.5 週に対し、当日完了 (primitive 追加 § 2.6 A で
ゼロ、gap buffer string 表現の単純さで実装量が予想より少)。

** §3.2 Marker + overlay + text-property — SHIPPED 2026-04-24

目的: buffer 内位置参照と属性表現。

- [X] =src/nelisp-buffer.el= 拡張 (§3.1 の 230 LOC → ~430 LOC):
  - =nelisp-buffer= struct に =text-properties= slot 追加
  - =cl-defstruct nelisp-marker= (buffer / position / insertion-
    type) + =cl-defstruct nelisp-overlay= (buffer / start / end /
    front-advance / rear-advance / props)
  - shift helpers を cons-cell placeholder から struct base に
    差し替え、=insert= / =delete-region= / =erase-buffer= で
    markers / overlays / text-properties を三位一体に更新
- [X] Marker API 5 本: =nelisp-markerp= / =nelisp-make-marker=
  (detached) / =nelisp-copy-marker= (buf 内作成) /
  =nelisp-set-marker= (cross-buffer 移動 + nil で detach) /
  =nelisp-marker-delete= (unlink)
- [X] Overlay API 7 本: =nelisp-overlayp= / =nelisp-make-overlay=
  (front/rear-advance 指定可) / =nelisp-delete-overlay= /
  =nelisp-overlay-put= / =nelisp-overlay-get= /
  =nelisp-overlays-at= / =nelisp-overlays-in=
- [X] Text-property API 4 本 (sparse list、interval tree 回避):
  =nelisp-put-text-property= (newest-first stack) /
  =nelisp-get-text-property= (=plist-member= で "nil 設定" を
  "未設定" と区別) / =nelisp-text-property-intervals= /
  =nelisp-remove-text-properties=
- [X] =test/nelisp-marker-test.el= 新設 (29 ERT):
  - 2 marker struct / basic
  - 4 marker insert shift (before / after / at:stay / at:advance)
  - 3 marker delete shift (after / before / covering)
  - 3 marker cross-buffer / detach / delete
  - 5 overlay (make / put-get / overlays-at / overlays-in /
    delete)
  - 4 overlay shift (insert-inside-expands / at-start 2 variants
    / delete-covering / delete-before)
  - 6 text-property (put-get / newest-wins / nil-distinguishable
    / shift-on-insert / shift-on-delete / remove)
  - 1 erase-buffer が text-properties を clear
- [X] make test: 609 → *638/638 green* (+29, 0 unexpected)
- commit: =eb8194c= on =feat/phase5b.2-marker=

期待スコープ 1.5 週に対し当日完了 (§3.1 で struct 土台が既に
整っており、shift helpers の拡張で済んだ)。

** §3.3 Redisplay (terminal, dirty-region) — SHIPPED 2026-04-24

目的: NeLisp-buffer の内容を terminal grid に描画。

実装 (commit 9a2cd8a):
- =nelisp-emacs= 兄弟リポジトリの =src/nelisp-redisplay.el= 新設 (~135 LOC)
- =(cl-defstruct nelisp-window buffer top-line height width
  cursor-row cursor-col dirty-rows)=
- 公開 API: =nelisp-make-window= / =nelisp-window-mark-dirty=
  / =nelisp-window-mark-all-dirty= / =nelisp-redisplay-window=
  / =nelisp-redisplay-clear-screen= / =nelisp-redisplay-terminal-size=
- dirty rows は昇順 sort 後に escape+text を emit、paint 後に
  dirty list を nil クリア、末尾で cursor 位置を restore
- VT100 escape: CSI =\e[= + =row;colH= (CUP) / =2K= (EL) / =2J=
  (ED) を defconst で統一
- テスト可能化: =nelisp-redisplay--output-fn= defvar を
  =#'send-string-to-terminal= で初期化、ERT は capture closure
  に let-bind して *stdout を触らず* 検査可能

ERT (=test/nelisp-redisplay-test.el=, 18 本):
- struct defaults / custom size (2)
- mark-dirty basic / idempotent / mark-all-dirty (3)
- emits goto-xy / clear-line / content (3)
- width-clamp / empty-row pad / out-of-range skip (3)
- dirty rows ascending order paint (1)
- dirty list cleared post-paint (1)
- top-line scroll offset (1)
- cursor position restored after paint (1)
- clear-screen emits 2J + home (1)
- nil buffer no-crash (1)
- terminal-size (width . height) (1)

期待スコープ: 2 週。実績: 当日 (§3.1/§3.2 の構造定着 + sketch
が完全に揃っていたため、sketch を本実装化するのみで済んだ)。

** §3.4 Event loop (actor integration) — SHIPPED 2026-04-24

目的: input event → buffer update → redisplay の flow。

実装 (commit 483e1ef):
- =src/nelisp-eventloop.el= 新設 (~180 LOC)
- =cl-defstruct nelisp-event= (kind / data / timestamp) +
  =nelisp-make-event= コンストラクタ
- key-sequence 正規化 helper =nelisp-eventloop--key-seq=
  (int → =char-to-string=、string verbatim、他は =format=)
- 公開 API:
  - binding: =-bind= / =-unbind= / =-lookup= / =-reset-bindings=
  - 同期 queue: =-enqueue= / =-step= / =-drain= /
    =-run-scripted= (FIFO + quit 途中終了対応)
  - actor 統合: =nelisp-eventloop-spawn-main-actor= (Phase 4
    =nelisp-actor-lambda= body で =nelisp-receive= → dispatch
    → =nelisp-yield= を loop、=quit= event で actor 終了)
  - timer: =nelisp-eventloop-schedule-timer= (host =run-at-time=
    経由で TARGET actor に timer event 送信、=cancel-timer=
    可能なように host timer object を return)
- §2.5 B 方針: Phase 4 actor 再利用、別 thread / tick system
  は作らない。main event actor 単一、timer は host run-at-time
  借用
- tty raw-mode stdin 実入力は scope 外。pluggable
  =nelisp-eventloop-input-source= defvar を用意しておき §3.5
  E2E demo で scripted feed へ差し替え可能
- command 契約: 1-arg function、dispatched event を受け取る。
  buffer / window は closure 経由 or dynamic var (§3.5 で実装)

ERT (=test/nelisp-eventloop-test.el=, 20 本):
- event 構造 (kind/data/timestamp、defaulted timestamp) (2)
- key-seq 正規化 int / string (2)
- bind / unbind / reset / lookup (4)
- dispatch key / unbound / timer / quit / unknown (5)
- sync queue enqueue+step / drain / run-scripted FIFO /
  run-scripted stops on quit (4)
- main actor dispatches key / interleaves many / quit
  terminates (3)
- =schedule-timer= routes timer event to target actor (sit-for
  loop で host timer fire を await) (1)

期待スコープ: 2 週。実績: 当日 (Phase 4 actor 基盤が既に完成、
dispatch core + queue + actor wrapper の thin 層で済んだ)。

** §3.5 E2E demo — NeLisp-editor minimum — SHIPPED 2026-04-24

目的: 3 核連動で最小 editor 動作、§2.9 C 達成。

実装 (commit 59811fe):
- =examples/nelisp-editor/main.el= 新設 (~200 LOC):
  =(require 'nelisp-buffer)= + =(require 'nelisp-redisplay)= +
  =(require 'nelisp-eventloop)= で 3 核連動
- 公開 API: =nelisp-editor-init= / =-install-bindings= /
  =-run-scripted= / =-run-actor-scripted= / =-repaint= +
  current-buffer / -window accessor
- 8 command: =insert-char= (printable + =\n=) / =forward-char=
  / =backward-char= / =next-line= / =previous-line= /
  =delete-char= / =delete-backward-char= / =quit=
- key binding 最小 set: 32..126 + =\n= = self-insert、=C-f= /
  =C-b= / =C-n= / =C-p= / =C-d= / =DEL= (0x7f) /
  *C-q* (quit、=C-x C-c= 相当の multi-char prefix は scope 外、
  後続 Phase の prefix-handling で対応)
- cursor 追跡: point → (row, col) を buffer text 走査で再計算
  (top-line=1 fixed、scroll は現 MVP 範囲外)、command 後に
  window mark-dirty、redisplay 経路で visible
- line 編集: =nelisp-editor--line-edges= で (start . end) を
  列挙、next/previous-line は column preserve で target 計算
- =examples/nelisp-editor/README.org= 新設: 起動方法 (ERT /
  対話) + bindings 表 + hello package demo 手順 + ASCII
  architecture 図 + non-scope 列挙

ERT (=test/nelisp-editor-test.el=, 13 本):
- init buffer + window 生成 (1)
- self-insert single / sequence / newline (3)
- forward/backward bounded (1)
- delete-char forward / backward (2)
- next / previous line column preserve (1)
- quit flag clears (1)
- actor-scripted builds string / main terminates on quit (2)
- *hello-package integration*: =nelisp-require 'hello= →
  =nelisp-eval '(hello-world)= → "Hello, World!" を 1 char
  ずつ insert → buffer 一致 + point 末尾 (1)
- redisplay after insert emits CSI + text (1)

期待スコープ: 1 週。実績: 当日 (§3.1-3.4 の 3 核実装が揃って
いたため、薄い glue 層で demo が成立)。release bar 後半 §2.9
C (= 3 核連動で visible editor 動作) ここで達成。

** §3.6 Merge gate — SHIPPED 2026-04-24

全 sub-phase SHIPPED + make test-all green + E2E demo 動作で
main merge 完了。audit 結果:
- [X] NeLisp-buffer が host buffer parity test で semantics
  一致 — =test/nelisp-buffer-test.el= 内 5 parity ERT pass
  (insert / delete / point / goto-char / buffer-string)
- [X] redisplay が dirty-row incremental (全画面 redraw で
  ない) 動作証拠 — =nelisp-redisplay-dirty-rows-emitted-in-
  ascending-order= + =-empty-row-padded-blank= が dirty
  set のみ paint されることを regex で検証、=-dirty-rows-
  cleared-after-paint= で post-paint 状態確認
- [X] event-loop から key → insert → redisplay の chain が
  actor tick 内で完結 (blocking call 無し) — =nelisp-editor-
  actor-scripted-builds-string= で actor main loop 経由で
  key → insert が完遂、=nelisp-editor-redisplay-after-insert=
  で同 buffer 状態から escape+text emit を capture hook
  で確認
- [X] ERT 追加 合算 +120 (§3.0 +11 / §3.1 +29 / §3.2 +29 /
  §3.3 +18 / §3.4 +20 / §3.5 +13、目標 +60 を大幅超過)
- [X] Phase 5-A 569 本 + 新規全 pass — =make test= 569 →
  *689/689 green, 0 unexpected*

merge 順 (Phase 5-A と同じ =--no-ff= で 8 bubble):
1. =feat/phase5b-doc-draft= (dc411c4 + 26ced87 — Doc 13 DRAFT
   + §2 LOCKED)
2. =feat/phase5b.0-pre-req= (e966a91 + e4c8e3a — 10 primitive
   + audit infra)
3. =feat/phase5b.1-buffer= (3b59c30 + aa3e82f — gap buffer +
   29 parity ERT)
4. =feat/phase5b.2-marker= (eb8194c + 11a473a — marker /
   overlay / text-property)
5. =feat/phase5b.3-redisplay= (9a2cd8a + a9ef7e1 — terminal
   dirty-region MVP)
6. =feat/phase5b.4-eventloop= (483e1ef + 3867292 — actor
   integration + timer)
7. =feat/phase5b.5-editor-demo= (59811fe + 07af673 — 3 核連動
   + hello package integration)
8. =feat/phase5b.6-merge-gate= (本 commit — audit checkmarks +
   top STATE SHIPPED)

* §4 期待成果と next

*** Phase 5-B 完了後の状態
- NeLisp が自前の buffer / redisplay / event-loop を持ち、
  host Emacs に依存せず最小 editor として動作
- host C に残る依存は Phase 0 decision の SBCL 4 核 (syscall
  / allocator / GC inner / coding) に限定、buffer/redisplay/
  event-loop の C コードへの runtime 依存は 0
- release bar 後半達成: "Emacs core 置換" 完了
- v1.0 完遂条件 4 項のうち 1 + 2 が達成 (残: 3 AI-critical
  path / 4 SBCL 4 核絞り込み audit)

*** Phase 5-C 以降への橋渡し
- Phase 5-C: process / network 置換 (=make-process= /
  =open-network-stream= の NeLisp 版)。anvil.el の
  =make-process= 罠 (pipe で session leader / fd 0 controlling
  tty inherit) を structural に解決
- Phase 5-D: file-notify / watcher の actor 化 (Windows
  freeze 解消)
- Phase 6: GUI redisplay、mode 基盤、履歴
- v1.0 tag: Phase 5-B + 5-C + AI-critical 5 領域の dog-food
  成功後

*** Out of scope
- GUI redisplay (X / Windows / Cocoa) — Phase 6+
- Network / process / file-notify — Phase 5-C+
- Unicode 完全対応 / bidi / ligature — Phase 7+
- Major / minor mode 実装互換 — substrate 揃えば Elisp で
  可能、本 Phase 範囲外
- Emacs C への既存 user-visible API (=switch-to-buffer= 等)
  の 1:1 port — NeLisp namespace (=nelisp-*=) 限定

* §5 User 判断事項 (signoff) — LOCKED 2026-04-24

本 doc の §2 decisions 9 項目 user 承認済 (2026-04-24)、全推奨で LOCK:

| § | 項目 | 推奨 | 備考 |
|---+------+------+------|
| 2.1 | Buffer data structure | A | Gap buffer (Emacs 同型) |
| 2.2 | Text representation | A | host =string= 流用 |
| 2.3 | Marker / overlay | A | linked list MVP |
| 2.4 | Redisplay 戦略 | B | terminal + dirty-region |
| 2.5 | Event loop | B | Phase 4 actor 再利用 |
| 2.6 | Host Emacs 共存 | A | Parallel (=nelisp-buffer-*=) |
| 2.7 | Concurrency | A | single-threaded MVP |
| 2.8 | Test strategy | B | ERT + host parity test |
| 2.9 | Merge gate | C | 3 核連動 E2E demo 達成 |

signoff 完了 → =feat/phase5b.0-pre-req= branch で §3.0 着手。
全 §3 完了期待 = 10〜12 週 (§3.0 3d + §3.1 1.5w + §3.2 1.5w
+ §3.3 2w + §3.4 2w + §3.5 1w + §3.6 1d + buffer 週数 2w)。
