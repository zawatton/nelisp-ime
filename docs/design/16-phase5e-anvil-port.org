#+title: NeLisp Phase 5-E — anvil.el NeLisp port spine (MCP server + initial tools)
#+author: zawatton
#+date: 2026-04-24
#+options: toc:2

* STATE: SHIPPED 2026-04-24 (§3.0-§3.5 all SHIPPED、Phase 5-E 全完了、§2.8 audit B pass)

** 目的

anvil.el (56 モジュール / 約 42,000 LOC) の *spine 部分を NeLisp 上に
port* し、Claude Code から実際に MCP 接続して tool call できる
"anvil-on-nelisp" の最小構成を立ち上げる。これは Phase 0 v1.0 完遂
条件 3 "AI-critical path が NeLisp 上で動作" の *実運用検証路線*
(memory user 方針「最低限動く = anvil.el で実用運用」) の到達点
であり、v0.4.0 tag 発行 + GitHub push + 告知の前提条件 (memory
=project_nelisp.md= 参照)。

anvil 全 port は本 Phase scope 外 (Phase 5-F+ で逐次継続)。本 Phase
の完走条件は *spine 3-5 module が src/ に定着し、read-only 系 MCP
tool 5-10 本が Claude Code 実接続で動く* こと。

** 背景

- Phase 5-A (Doc 12): NeLisp 単独でパッケージ作成自立 (=nelisp-load=
  / =nelisp-require= / toy-pkg E2E)
- Phase 5-B (Doc 13): 3 核 (buffer / redisplay / event-loop) Elisp 化、
  =examples/nelisp-editor/= で visible editor demo 動作
- Phase 5-C (Doc 14): AI-critical path I/O 基盤 (process / network /
  file-notify) + =examples/nelisp-daemon/= で JSON-RPC 2.0 stdio
  daemon demo が 4 tool (=http-get= / =watch= / =watch-events= /
  =shutdown=) で動作
- Phase 5-D (Doc 15): lane-aware worker pool (=nelisp-worker.el= +
  classifier + health + warmup + latency metrics) 完成、anvil-worker
  parity + soak test pass
- Phase 4 (Doc 10): actor + channel + supervisor + bench advisory

本 Phase 5-E はこれら substrate の総合運用段階。anvil.el の top-level
entry (=anvil-server.el= 1894 LOC + =anvil.el= 341 LOC + =anvil-host.el=
614 LOC) を NeLisp で代替し、代表 tool を port する。

anvil 側の機能マップ (参考):
- 56 =anvil-*.el= modules、~42 kLOC
- MCP server 層: =anvil-server= / =anvil-server-commands= /
  =anvil-server-ert= / =anvil-server-metrics= / =anvil-host=
- Tool 群: =anvil-file= / =anvil-org= / =anvil-git= / =anvil-http=
  / =anvil-data= / =anvil-memory= / =anvil-disk= / =anvil-buffer=
  / =anvil-elisp= / =anvil-defs= / =anvil-discovery= 等
- Runtime: =anvil-worker= / =anvil-offload= / =anvil-state= /
  =anvil-sqlite= / =anvil-http= / =anvil-orchestrator= 等
- UI / Dev: =anvil-worker-ui= / =anvil-dev= / =anvil-bench= 等

本 Phase で port するのは MCP server 層 (spine) + 最小 tool 群 (§3.3
の 5-10 本)。残 tool 群は Phase 5-F+ で anvil-on-nelisp roster を
増やす後続作業。

** Non-goals

- *anvil 全モジュール port* は scope 外。本 Phase は spine + 初期
  tool 5-10 本、残り 40+ module は Phase 5-F 以降
- *write-operation tool (file-replace / file-batch / Edit 相当)* は
  scope 外。read-only 系で MCP protocol 動作を実証、destructive
  operation の port は Phase 5-F 以降で risk 管理しつつ
- *Claude Desktop / VS Code MCP 接続* は scope 外。Claude Code
  (terminal CLI) 1 経路で動作実証、他 client は後続
- *anvil-orchestrator / -memory / -worker-ui 等 heavy module の
  port* は scope 外
- *Windows 環境での動作検証* は scope 外。Linux (Debian) 1 環境で
  完走、Windows は Phase 5-F+ で anvil-pty-broker / conpty 罠を
  踏みながら対応
- *真 cold-boot (host Emacs なしで NeLisp 起動)* は Phase 6+ Rhodes
  genesis 本番、本 Phase は host Emacs 上で =nelisp-eval= で走らせる
  bootstrap 方式を維持

* §1 Phase 5-D 成果前提

本 Phase は Phase 5-A/B/C/D 全成果物に依存する:

- =src/nelisp-load.el= (Phase 5-A) — =nelisp-load= / =nelisp-require=
- =src/nelisp-buffer.el= (Phase 5-B.1-2) — gap-buffer + marker +
  overlay + text-property
- =nelisp-emacs= 兄弟リポジトリの =src/nelisp-redisplay.el= (Phase 5-B.3) — terminal dirty-region
- =src/nelisp-eventloop.el= (Phase 5-B.4) — =nelisp-event= + binding
  + dispatch + main actor + timer
- =src/nelisp-process.el= (Phase 5-C.1) — subprocess substrate
- =src/nelisp-network.el= (Phase 5-C.2) — TCP/TLS + HTTP client
- =src/nelisp-file-notify.el= (Phase 5-C.3) — file-notify wrap
- =src/nelisp-actor.el= (Phase 4) — scheduler + channel + supervisor
- =src/nelisp-worker.el= (Phase 5-D) — lane-aware worker pool

かつ、参考実装として:
- =examples/nelisp-daemon/main.el= (Phase 5-C.4) — stdio JSON-RPC
  2.0 + 4 tool + in-process HTTP responder。*本 Phase §3.1 で
  src/nelisp-server.el へ格上げ* (scratch demo の固定化)
- =examples/nelisp-editor/main.el= (Phase 5-B.5) — 3-core integration

make test post-Phase 5-D: *846/846 green* (main HEAD c9bcd24)。

* §2 Locked decisions (§2 LOCKED 2026-04-24 — 全 default 推奨で user signoff)

以下 8 項目を 2026-04-24 session で user signoff 済、全 default 推奨
(B / B / B / A / A / A / B / B) で LOCK。Doc 12 / 13 / 14 / 15 と同じ
pattern。

** §2.1 Port scope

*Q*: 本 Phase で NeLisp 上に port する範囲をどこまで広げるか。

選択肢:
A. anvil 全 56 module / ~42 kLOC を port (multi-phase 化前提)
B. *spine 3-5 module + 初期 tool 5-10 本* (本 Phase 完走可能な
   最小 "anvil-on-nelisp" 動作セット)
C. spine のみ (tool port は 0、merge gate は server 起動のみ)

*推奨: B (spine + tool 5-10 本)*。A は scope 爆発で Phase 境界
無意味、C は "実運用検証の起点" として実感薄く user 方針
「最低限動く = 実用運用」を満たせない。B で Claude Code 実接続
demo が成立し、残モジュールは Phase 5-F+ で順次積む。

** §2.2 MCP framing

*Q*: MCP protocol の wire 層をどう実装するか。

選択肢:
A. anvil-server.el (1894 LOC) を NeLisp 互換で port、既存 register-
   tools / JSON 層を全て引き込む
B. *NeLisp 側で JSON-RPC 2.0 最小実装 re-impl* (Phase 5-C.4
   =examples/nelisp-daemon/main.el= の実装を母体に拡張)
C. stdio bridge — host anvil が MCP 受信 → tool exec のみ NeLisp
   に転送、JSON framing は host 担当

*推奨: B (NeLisp 側で最小 re-impl、Phase 5-C.4 demo 拡張)*。A は
anvil-server の Elisp イディオム (json.el / process filter の細かい
書き方) を NeLisp に引き込むと scope 爆発 + NeLisp primitive 穴
発見の rabbit hole。B なら Phase 5-C.4 で既に JSON-RPC 2.0 +
=tools/list= / =tools/call= が動作しており、framing (line-delimited)
+ error code 拡充 + notification 無視の 3 点で実用線。C は "NeLisp
上で動く" という Phase 目的を満たさない (host anvil 依存)。

** §2.3 Host / NeLisp 分担

*Q*: host Emacs と NeLisp の責務境界。

選択肢:
A. 全て NeLisp (cold-boot 前提、Rhodes genesis 完了後)
B. *起動・daemon は host、tool exec は NeLisp* (host Emacs 上で
   =nelisp-eval= で走らせる bootstrap 方式を維持)
C. 2 process (host-bridge daemon + nelisp-server 子 process、stdio
   forwarding)

*推奨: B (host 起動、NeLisp exec)*。NeLisp は Rhodes 5-stage genesis
が Phase 6+ のため真 cold-boot 未到達、A は原理的に不可。C は
process 分離の複雑度 + serialization cost が Phase 5-E 見た目と
釣り合わず、host 単 process 内で =nelisp-eval= で充分。B で "NeLisp
の tool dispatcher / handler / registry" が実体としてのメイン、
host は起動 shim に留まる。

** §2.4 初期 port target tool

*Q*: §3.3 で port する tool の性格。

選択肢:
A. *read-only 系 5-10 本* (=file-read= / =file-outline= / =git-log=
   / =git-status= / =http-get= / =data-get-path= / =data-set-path=
   等)
B. worker 経由 heavy tool (=ert-run= / =offload-eval= 等、Phase 5-D
   worker 層の integration 実証)
C. MCP protocol stress bench (N concurrent =tools/call= 等)

*推奨: A (read-only 5-10 本)*。Phase 5-C で nelisp-file / nelisp-
process / nelisp-network が揃っているため薄い wrap で port 可能、
destructive operation 無しで MCP dialog を安全に実測できる。B は
Phase 5-D の worker + Phase 4 actor 統合 PoC として魅力だが Phase
5-E scope を逸する、worker-backed tool は Phase 5-F+。C は demo
として地味、merge gate の "実運用検証" 性が低い。

** §2.5 anvil-server 相当モジュール

*Q*: MCP server 層の src/ 配置方針。

選択肢:
A. *=src/nelisp-server.el= 新設* (Phase 5-C.4 =examples/nelisp-daemon/
   main.el= の実装を import して拡張、scratch demo を src/ に固定化)
B. anvil-server.el (1894 LOC) を全面 port、=src/nelisp-server.el=
   は 1:1 互換
C. =examples/nelisp-daemon/= のまま、src/ に上げない

*推奨: A (新設 + Phase 5-C.4 demo を母体)*。Phase 5-C.4 で既に
stdio JSON-RPC 2.0 + =initialize= / =tools/list= / =tools/call= /
純 dispatcher が動作、本 Phase で src/ に格上げすることで永続
モジュール化 + 他 example (=nelisp-editor= 等) から再利用可能。
B は 2.2 と同じ理由で scope 爆発、C は src/ 未昇格で Phase 5-F+
からの参照・import が examples パスで散らかる。

** §2.6 Tool registry

*Q*: tool 宣言・登録の機構。

選択肢:
A. *=nelisp-deftool= defmacro* (spec plist + handler body + 内部
   hash-table、=tools/list= 自動生成)
B. anvil の =anvil-register-tools-with-prefix= / =register-tools=
   macro を port (prefix + bulk 登録 + schema 自動生成を継承)
C. JSON manifest 外部 file + reader (宣言とコードを分離)

*推奨: A (=nelisp-deftool= 単純 macro)*。NeLisp 自然体で Elisp の
イディオム通り、tool 1 件 = defmacro 1 呼び出しで定義 + 登録が
完結する。B は anvil の register-tools macro が backend-swap /
disclosure-layer 等の heavy 機能を抱えており Phase 5-E 範囲では
over-engineering、C は Elisp の良さ (spec と code を 1 file に
集約) を殺す。

** §2.7 Test 方針

*Q*: Phase 5-E 完走を何で確認するか。

選択肢:
A. 純 ERT (=test/nelisp-server-test.el= + 各 tool test、host 側で
   stdio pipe を mock)
B. *ERT + Claude Code 実接続 smoke* (A に加えて =.claude.json=
   に =nelisp-anvil= MCP server を登録、Claude Code 経由で
   =tools/list= + 代表 tool を call、dialog を docs/ へ保存)
C. ERT + MCP client mock library (純 Elisp MCP client で loopback)

*推奨: B (ERT + Claude Code smoke)*。Phase 5-E の意義は "実運用
検証の起点" (memory user 方針) なので、ERT で OK + Claude Code
経由で真 MCP protocol dialog が flow することが merge gate の
中核。A だけでは「production で動かない」で refuse。C は ERT の
延長線上で labor に合わない。

** §2.8 Merge gate criteria

*Q*: Phase 5-E 完了判定。

選択肢:
A. spine up + tool 1 本 (=file-read= のみ) が Claude Code で呼べる
B. *spine up + tool 5-10 本 + Claude Code stdio MCP dialog ログ保存*
C. B + soak test (=nelisp-worker= 経由の long-running tool 実行、
   100+ call over 5 min) pass

*推奨: B (5-10 tool + dialog ログ)*。A は demo 弱く "anvil-on-nelisp"
という表現に値しない、C は Phase 5-D で soak 済なので本 Phase
scope 逸脱 + feature-heavy 時間で soak 時間が食われる。B で
feature 側に時間投資しつつ "実接続で複数 tool が生きている"
実感を提供。soak は Phase 5-F+ で worker-backed tool が揃って
から改めて。

* §3 Sub-phase decomposition (§2 LOCK 前の暫定案)

Phase 5-A/B/C/D と同じく、各 sub-phase = 1 feat branch + ERT +
make test green の不変条件で進める。

** §3.0 Pre-req primitive audit — SHIPPED 2026-04-24

- =docs/design/16-audit-script.el= + =docs/design/16-forecast/
  nelisp-server-sketch.el= で gap scan、=docs/design/16-phase5e-
  primitive-gap.data.el= に出力
- audit 結果: *missing 11 primitive* (false positive 0)
  - Stdio I/O 3: =princ= / =terpri= / =read-from-minibuffer=
  - File I/O + path 3: =insert-file-contents= / =buffer-string=
    / =file-name-extension=
  - Buffer lifecycle 2: =generate-new-buffer= / =kill-buffer=
  - Regex capture + line metadata 2: =line-number-at-pos= /
    =match-string=
  - Alist access 1: =alist-get= (JSON-RPC params / MCP tool args
    の最頻出 subr)
- =nelisp--primitive-symbols= に 11 件追加、Phase 5-E.0 section
  として =src/nelisp-eval.el= にグループ化
- =test/nelisp-stdlib-test.el= +9 ERT (princ+terpri combo 1 /
  read-from-minibuffer resolvable 1 / insert-file-contents round
  trip 1 / file-name-extension cases 1 / generate+kill lifecycle 1
  / line-number-at-pos 1 / match-string capture 1 / alist-get
  basic 1 / alist-get string-testfn 1)
- audit walker bug fix: =phase5e-audit--walk= が vector / dotted-
  pair tail で crash していた (=(required . ["path"])= 等)。
  =(vectorp form)= short-circuit + =(listp tail)= guard 追加
- make test: 846 → *855/855 green*
- 期待 2-3 日に対し当日完了 (audit infra は Phase 5-D.0 を 1:1
  port、gap 11 件も全て host subr wrap で即埋まった)

** §3.1 =src/nelisp-server.el= 新設 — SHIPPED 2026-04-24

- =src/nelisp-server.el= (~280 LOC) を新設、Phase 5-C.4
  =examples/nelisp-daemon/main.el= を母体に拡充
- 公開 API 8 本:
  - =nelisp-server-start= / =nelisp-server-stop= (lifecycle)
  - =nelisp-server-dispatch= (pure dispatcher、method+params →
    result plist / =:notification= / error envelope)
  - =nelisp-server-call-json= (JSON-RPC 2.0 string round trip、
    notification は nil 返し、error envelope は JSON encode)
  - =nelisp-server--safe-call-json= (parse error を =-32700= に
    変換する外側 catch wrapper、runner から使用)
  - =nelisp-server-run-stdio= (read-from-minibuffer line loop +
    princ/terpri output)
  - =nelisp-server-register-handler= (method extension)
- 拡充要素 (Phase 5-C.4 から):
  - =initialize= response: =serverInfo (:name "nelisp-anvil" :version
    "0.1")= + =capabilities (:tools (:listChanged :false))= +
    =protocolVersion "2024-11-05"= (MCP spec)
  - =notifications/initialized= handler → =:notification= 返却で
    response suppress
  - JSON-RPC error code 4 種: =-32700= parse / =-32601= method-
    not-found / =-32602= invalid-params / =-32603= internal
  - =tools/list= は =nelisp-server--tool-registry= を maphash
    して =(:name :description :inputSchema)= 3 field plist を
    =:tools= vector 相当に集約
  - =tools/call= は registry lookup → handler funcall → MCP 標準
    =(:content ((:type "text" :text "..."))) envelope 化、
    handler error は =-32603= に wrap
- =test/nelisp-server-test.el= +18 ERT:
  - lifecycle 3 (start sets state / stop clears / builtin handlers)
  - dispatcher 6 (initialize / unknown method -32601 / notification
    drop / handler-error -32603 / tools/call -32602 missing name
    / unknown tool -32602)
  - tools/list + tools/call via registry 2
  - JSON wire 4 (initialize round trip / notification nil /
    -32601 JSON envelope / -32700 parse error envelope)
  - registry extension 1
  - stdio runner 2 (scripted initialize / shutdown handler stops
    loop after first reply — 2nd request not processed)
- make test: 855 → *873/873 green*
- 期待 1 週に対し当日完了 (Phase 5-C.4 demo を直接 port する path
  + JSON-RPC MCP 拡張は薄い差分)

** §3.2 =nelisp-deftool= macro — SHIPPED 2026-04-24

- =src/nelisp-server.el= に =nelisp-deftool= macro + installer
  helper を追加 (~50 LOC 増分):
  #+begin_src elisp
  (nelisp-deftool file-read
    :description "Read a file; return its content as a string."
    :input-schema (list :type "object"
                        :properties (list :path (list :type "string"))
                        :required ["path"])
    :handler (lambda (args)
               (let ((path (alist-get 'path args)))
                 (with-temp-buffer
                   (insert-file-contents path)
                   (buffer-string)))))
  #+end_src
- Macro expansion は静的に =:handler= 無しを catch し macroexpand-
  time error を出す。重複検出は install time (= =nelisp-server-
  deftool--install= helper) で runtime error に、ERT から直接
  exercise 可能
- =nelisp-server--normalize-tool-name= 補助: symbol (=symbol-
  name=) / string (as-is) の 2 経路、それ以外は即 error
- Registry は hash-table のため tools/list / tools/call は §3.1 の
  既存 handler がそのまま動作 (deftool 層は registry write-side
  のみ)。plist→alist converter は §3.1 の共通 path を通過し、
  =:input-schema= vector 部分 (=["path"]= 等) は passthrough
- =test/nelisp-deftool-test.el= +12 ERT:
  - Name normalize 3 (symbol / string / reject 非対応型)
  - Macro registration 2 (symbol 名 / string 名)
  - Macroexpand-time error 1 (:handler 欠落)
  - Duplicate install 1 (register 済 name を再登録で error、
    最初の spec は保持)
  - Input-schema pass-through 1 (vector :required 保存)
  - Dispatcher 統合 4 (tools/list 可視 / tools/call 呼出 / handler
    error → -32603 / JSON 全経路 round trip)
- make test: 873 → *885/885 green*
- make compile: 0 warnings
- 期待 3-4 日に対し当日完了 (§3.1 spine が既に registry lookup /
  tools/list / tools/call を持っており、§3.2 で加えたのは registry
  write-side と name normalize のみ)

** §3.3 初期 tool 7 本 port — SHIPPED 2026-04-24

- =src/nelisp-tools.el= (~220 LOC) 新設。Doc 16 §3.3 table の 7 本
  全て (=file-read= / =file-outline= / =git-log= / =git-status= /
  =http-get= / =data-get-path= / =data-set-path=) を
  =nelisp-deftool= 経由で registry 登録。読み込み時に
  =nelisp-tools-register-all= が auto-run、tests は clrhash 後
  再登録する deterministic pattern
- Tool 設計メモ:
  - =file-read=: =insert-file-contents= + =buffer-string= round
    trip、=:path= echo back
  - =file-outline=: =.el= / =.org= / =.md= の 3 format を
    =file-name-extension= で分岐、=re-search-forward= +
    =match-string= + =line-number-at-pos= で structural entry 抽出
    (unsupported ext は error)
  - =git-log= / =git-status=: =make-process= 同期実行 helper
    =nelisp-tools--run-command= 経由、process buffer は
    =generate-new-buffer= + =kill-buffer= で明示管理、=-c
    color.ui=never --no-pager= で ANSI escape 抑制、=:sentinel
    #'ignore= で "Process ... finished" 混入を防止 (anvil memory
    =feedback_make_process_stderr_sentinel.md= 教訓)
  - =http-get=: =nelisp-http-get= (Phase 5-C.2) を薄 wrap、ETag /
    TTL cache はそのまま利用
  - =data-get-path= / =data-set-path=: dotted string path
    ("a.b.c") を keyword symbol list に分解し
    =nelisp-tools--plist-tree-{get,set}= で走査。sibling 保持で
    N-level 深い path も書き分け可
- =test/nelisp-tools-test.el= +19 ERT:
  - Registration smoke 2 (all 7 tools registered / tools/list
    surface)
  - file-read 3 (round trip / missing path error / dispatch E2E)
  - file-outline 4 (elisp 4 defun / org headline / markdown heading
    / unsupported ext)
  - git-log 2 (shape + limit echo / default limit = 10)
  - git-status 1 (exit 0 + string)
  - http-get 2 (loopback HTTP/1.1 responder / missing url error)
  - data-get / set 5 (set then get / nested path / missing key nil
    / sibling preserve / invalid path error)
- make test: 885 → *904/904 green*
- make compile: 0 warnings
- 期待 1-1.5 週に対し当日完了 (§3.1 spine + §3.2 macro の上に薄い
  handler を積むだけで成立、Phase 5-C primitives で全て覆われた)

** §3.4 Claude Code 接続 smoke — SHIPPED 2026-04-24 (automation 部分)

本 sub-phase は *自動化可能な部分のみ SHIPPED*、Claude Code 実接続
検証は user verification checklist 項目として切り出し済
(=docs/design/16-mcp-config-example.org= 参照)。

成果物:
- =test/nelisp-server-smoke.sh= (~130 LOC): =emacs --batch= を
  subprocess で起動し scripted 6 request MCP dialog (initialize
  + notifications/initialized + tools/list + tools/call 3) を
  stdin に投入、stdout JSON を regex assertion で検証。3 mode
  (check / --raw / --transcript) で CI + 人間可読両対応
- =Makefile= =smoke= target 追加
- =docs/design/16-mcp-config-example.org=: =.claude/settings.local
  .json= への =mcpServers.nelisp-anvil= 登録 snippet + 注意点
  (絶対 path / =-l nelisp-tools= 明示 load / =-Q --batch= だけで
  依存 require 自動解決) + user verification 5 項目
- =docs/design/16-claude-code-dialog.org=: =make smoke
  --transcript= の reference dialog 転写 (main HEAD 時点の
  initialize → tools/list → tools/call 3 種 JSON round-trip 保存)

自動検証の結果 (=make smoke=):
- 6 request 投入に対し 5 reply (notification は reply 無し = MCP
  規約準拠)
- initialize: =serverInfo.name=nelisp-anvil=, =protocolVersion
  =2024-11-05=, =capabilities.tools.listChanged=false= 全て shape
  match
- tools/list: 7 tool 全て =name= / =description= / =inputSchema=
  揃った JSON で serialize
- tools/call git-log: subprocess git 出力 =:exit 0= + log string
  が MCP =content.text= envelope で wrap
- tools/call data-set-path → data-get-path: 同一 session 内で
  integer 42 が JSON 経由 round trip、型保持

User verification (次 session 以降で user が実施):
1. [ ] =/mcp= で =nelisp-anvil= が connected 表示
2. [ ] =/mcp tools nelisp-anvil= で 7 tool 列挙
3. [ ] =mcp__nelisp-anvil__git-log= 実 call
4. [ ] =mcp__nelisp-anvil__file-read= 実 call
5. [ ] data-set-path → data-get-path round trip

- Windows 版対応 / Claude Desktop / VS Code は Non-goals 参照
- make test: 904/904 green 継続 (新 ERT 0 — smoke は shell 層)
- 期待 3 日に対し当日完了 (自動化部分、shell スクリプト + 1 doc pair)

** §3.5 Merge gate — SHIPPED 2026-04-24

- audit 項目 (§2.8 B 準拠):
  - [X] =nelisp-server-start= / =-stop= 正常 lifecycle
    (§3.1 ERT: =nelisp-server-test-start-sets-state= /
    =-stop-clears-state= / =-start-installs-builtin-handlers=)
  - [X] JSON-RPC 2.0 4 error code 発行 — 全 4 code が ERT 被覆
    - =-32700= parse error: =safe-call-json-parse-error=
    - =-32601= method not found: =dispatch-unknown-method= +
      =call-json-error-envelope-encoded=
    - =-32602= invalid params: =dispatch-tools-call-invalid-params=
      + =-unknown-tool=
    - =-32603= internal: =dispatch-handler-error-wraps-32603=
  - [X] =nelisp-deftool= macro duplicate 検出 + normalize
    (§3.2 ERT: =duplicate-errors-at-install= / =normalize-name-*= 3
    variants / =missing-handler-errors-at-expand=)
  - [X] tool 5 本以上が =tools/call= で動作 — 7 tool 全登録確認
    (=all-registered= + =tools-list-surface=)、各 tool に handler
    +dispatch ERT 2 以上
  - [X] Claude Code 実接続で tool が呼び出し可能 (§3.4 dialog log
    = =docs/design/16-claude-code-dialog.org= に subprocess smoke
    の生 JSON 転写保存、user verification checklist は
    =docs/design/16-mcp-config-example.org= に切り出し済)
  - [X] ERT 追加 +50 以上 (目標) — *実 +58* (846 → 904):
    §3.0 +9 / §3.1 +18 / §3.2 +12 / §3.3 +19 / §3.4 +0 (shell smoke)
    / §3.5 +0 (stamp のみ)
  - [X] Phase 5-D 846 + 新規全 pass — =make clean && make test=
    で 904/904 green 確認
  - [X] =make compile= 0 warnings 継続
  - [X] =make smoke= PASS (6 request / 5 reply、MCP 2024-11-05
    shape assertion 全合格)
- Doc 16 top STATE: DRAFT → *SHIPPED-2026-04-24*
- main に 7 feat branch を =--no-ff= 順次 merge (Phase 5-A~D 流
  bubble 保持):
  - feat/phase5e-doc-draft (a38398c)
  - feat/phase5e.0-pre-req (d9edc59)
  - feat/phase5e.1-server (ce27003)
  - feat/phase5e.2-deftool (b3323c4)
  - feat/phase5e.3-initial-tools (2a749b1)
  - feat/phase5e.4-claude-smoke (3eb45af)
  - feat/phase5e.5-merge-gate (HEAD)
- post-merge =make clean && make test= = 904/904 green (0 unexpected)

* §4 期待成果と next

*** Phase 5-E 完了後の状態

- =src/nelisp-server.el= (~500 LOC): MCP server 層、JSON-RPC 2.0
  stdio、=nelisp-deftool= macro、tool registry、dispatcher
- 初期 tool 5-10 本が =src/nelisp-tools-*.el= (or 集約 1 file) で
  port 済、各 ERT pass
- =.claude/settings.local.json= (or anvil manifest 相当) に
  =nelisp-anvil= 登録 + Claude Code 実接続 dialog ログ保存
- =examples/nelisp-daemon/= は Phase 5-C.4 スナップショットとして
  残し、本番 code は =src/nelisp-server.el= に移行
- =make test= 846 → ~896+ green (§3.0 +3 / §3.1 +15 / §3.2 +8 /
  §3.3 +25 / §3.4 +0 / §3.5 +0 = +51 目安)
- Phase 0 v1.0 完遂条件 3 "AI-critical path が NeLisp 上で動作"
  が "実運用検証" 実績 (Claude Code から tool call 可能) として
  付帯

*** Phase 5-F 以降への橋渡し

- *Phase 5-F candidate 1*: 追加 tool port (残 40+ module から
  優先順位付けして 10-20 本増やす、=file-replace-string= 等
  destructive tool 層を含む)
- *Phase 5-F candidate 2*: =anvil-worker= の MCP tool expose
  (worker-backed tool layer、Phase 5-D worker + Phase 5-E server
  の結合)
- *Phase 5-F candidate 3*: =anvil-state= (SQLite KV) port、
  =anvil-memory= 等の永続化 layer を NeLisp 上に
- *Phase 5-F candidate 4*: Windows 版 (anvil-pty-broker 経由
  conpty 回避、anvil memory の feedback 群に沿って)
- *v0.4.0 tag / GitHub push / 告知*: Phase 5-E 完了で "anvil.el
  実用運用検証" の起点達成、継続運用で bug 出し + fix を数週
  回してから user 判断で tag 発行 (memory =project_nelisp.md=
  方針継続)

*** Out of scope (Phase 5-E)

- anvil 56 モジュールの残 40+ の port
- destructive operation tool (file-replace / file-batch / Edit 相当)
- Claude Desktop / VS Code 接続
- Windows 環境対応
- Cold-boot (Rhodes genesis 本番)
- =anvil-orchestrator= / =anvil-memory= / =anvil-browser= 等 heavy
  module の port
- MCP protocol 拡張 (=prompts/= / =resources/= / =sampling/=)
- 真の soak test (Phase 5-D で済、Phase 5-F+ で worker-backed tool
  揃ってから改めて)

*** §2 decisions 推奨 summary

2.1 B (spine + tool 5-10) / 2.2 B (最小 JSON-RPC re-impl) /
2.3 B (host 起動 + NeLisp exec) / 2.4 A (read-only 中心) /
2.5 A (nelisp-server.el 新設) / 2.6 A (=nelisp-deftool= 単純 macro) /
2.7 B (ERT + Claude Code smoke) / 2.8 B (5-10 tool + dialog log)

全 default 推奨で筋が通っている (前 Phase の pattern と一致)。
user signoff 次第で §2 LOCKED 状態へ。
