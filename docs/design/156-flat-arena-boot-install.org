#+TITLE: Doc 156 — flat-arena cold-loader: production boot-INSTALL design
#+DATE: 2026-06-23
#+STATUS: INSTALL PROVEN + FAITHFUL (2026-06-23) — production cold-load install verified
#+STATUS:   end-to-end via the boot-wiring marker path (=/tmp/nelisp-cold-image.bin=).  value
#+STATUS:   cells + user defuns survive into the LIVE runtime and are callable.  Increment 4a
#+STATUS:   FIXED the native-override caveat (commit f781ce31): the cold path now skips
#+STATUS:   install-builtins, so prelude overrides of native builtins (A1 floor=3, A20 eq,
#+STATUS:   mod/truncate) survive identically to a normal boot.  Cold-load is now fully faithful.
#+STATUS: MULTI-CHUNK PROVEN (2026-07-04) — the single-chunk structural ceiling noted in
#+STATUS:   §4 Item 4 ("would need a multi-chunk dump + larger interned region") is lifted.
#+STATUS:   Increments MC-1 (streaming multi-chunk dump, commit 5eb0fd81) + MC-2 (multi-chunk
#+STATUS:   boot-INSTALL + =--cold-load-from PATH=, commit 1f486838) cold-load a 7-chunk,
#+STATUS:   ~602.5 MiB heap (~728 MB coalesced image) built from the 60-file Org vendor
#+STATUS:   chain: cold boot to first eval ~0.85 s vs ~9.8 s full =.repl= replay (~11.5x).
#+STATUS:   Increment 3 (2026-07-04) added a reproducible E2E harness
#+STATUS:   (=scripts/cold-image-org-e2e.sh=) and verified faithfulness: a replay-loaded and a
#+STATUS:   cold-booted process produce IDENTICAL (featurep/fboundp/commandp) conjunct columns.
#+STATUS:   The Increment-3 "featurep org = nil vs the make gate" caveat is RESOLVED
#+STATUS:   (2026-07-04, fix/e2e-repl-gate-parity): the harness probe wrapped conjuncts in
#+STATUS:   ignore-errors, which on this reader returns nil instead of the body value; bare
#+STATUS:   probes show featurep org / org-element = t in BOTH columns (see §7).
#+STATUS: ORG-MODE INSTALL CAVEAT CLOSED (2026-07-04, feat/org-cold-image-v2) — root cause was
#+STATUS:   the standalone reader dropping backquote-template macro invocations (the
#+STATUS:   define-derived-mode bridge), fixed upstream in nelisp-emacs-lib commit bd6d06d.
#+STATUS:   fboundp org-mode / commandp org-mode are now t in BOTH replay and cold-boot columns
#+STATUS:   under the DEFAULT (fast) prelude — no gate-prelude fallback needed. See §7 "v2
#+STATUS:   re-attempt".
#+BRANCH: spike/flat-arena-boot-install

* 0. Purpose

The flat-arena cold-loader (scripts/nelisp-flat-arena-probe.el, boot-wiring 5–14,
merged 439ac529) proves the dump→file→load→relocate→root-recovery MECHANISM, but
only as a READ-ONLY VERIFY (loads into a scratch =alloc-bytes= buffer; "live
runtime untouched").  The remaining work to make it usable (fast cold-load of a
loaded heap, ~9.2x vs .repl replay) is the production INSTALL: a fresh process
that loads an image into its REAL arena, relocates against the real base, installs
the EvalCtx roots, and skips the normal bootstrap.  This doc fully scopes that.

Motivation: the full nemacs bundle (=./bin/nemacs --driver=nelisp=, .repl replay)
cold-loads in 30–66 min and is OOM-prone (~11–15 GB RSS); package-breadth driving
needs the bundle (cl-lib/seq/pcase/macroexp/...).  A flat-arena image of the
loaded heap would load fast and unblock breadth — IF a one-time dump can be
created (the dump still needs ONE slow load; vendor-core subset is lighter).

* 1. What is proven (do not re-derive)

- Reader builtins wired (dispatch ~2499–2511, names list ~7495–7498):
  =nelisp--arena-dump-image-to-file= / =-dump-image-stream= / =-load-image-from-file=
  / =-boot-load-verify= / =-image-root-verify= / =-load-relocate-verify= /
  =-dump-table-verify= / =-dump-copy-verify= / =-load-split-verify=.
- Roundtrip works on the bare reader (verified 2026-06-23):
  =(nelisp--arena-dump-image-to-file "/tmp/arena.img")= then
  =(nelisp--arena-boot-load-verify "/tmp/arena.img")= → =(1 12 1 72)= =
  magic-ok / root-tag 12 (globals Record) / root-in-chunk0 / 72 block-total.

* 2. Image format (=bf_arena_dump_image_to_file=, header 64 B)

| off | field | source |
|-----+-------+--------|
| 0  | magic 1179407692 ("FLAT") | literal |
| 8  | slen (chunk-0 region bytes) | cursor − 1024 |
| 16 | isz (interned region bytes, 8-aligned) | abase+832 .. abase+840 |
| 24 | tlen (relocation-table entries) | nl_fa_roots + bf_arena_mr_chunks |
| 32 | gbox image-offset (globals root) | nl_img_off of env+8 |
| 40 | fbox image-offset (frames root)  | nl_img_off of env+40 |
| 48 | ubox image-offset (unbound root) | nl_img_off of env+72 |
| 56 | intern-region base (split-loader name-ptr fix) | abase-relative |

File body = [header][reloc table: tlen × u64][chunk-0 region: slen][interned
region: isz].  Reloc entry F: =image[F] += newbase= (chunk-0-relative pointers).
The interned region's open-addressing name-buffer ptrs (slot+8) live INSIDE the
interned region, so =nl_fa_field= (chunk-0-only) does NOT table them; they retain
absolute OLD pointers and must be fixed by base-delta on load (see the dump
comment + =bf_arena_load_split_verify=).

EvalCtx (env) layout (gdb-verified, from the dump + line ~2682):
- env reached via published safepoint ctx: =env = ptr-read-u64 (ctx+64)= when
  =ptr-read-u64 ctx 0 != 0=.
- globals box @ env+8, frames box @ env+40, unbound box @ env+72.

* 3. The remaining work: production INSTALL

** 3.1 New builtin =bf_arena_boot_install (args out)= (adapt =bf_arena_boot_load_verify=)

=bf_arena_boot_load_verify= already: opens file, reads header, reads table+image
into a SCRATCH =alloc-bytes img=, applies the reloc table (=image[f] += img=),
reconstructs a rootslot (tag 12, ptr = img+goff), verifies it is a valid Record.

INSTALL差分:
1. Load the chunk-0 region into the REAL arena chunk-0 data area (=sstart=, i.e.
   =arena_base + 0x400=) instead of a scratch buffer; load the interned region
   into the real interned region (=ib=).
2. Relocate against the REAL base delta (=newbase = sstart_new − slen_origin_base=),
   not the scratch base.  Apply the table AND the interned-region base-delta fix
   (mirror =bf_arena_load_split_verify=).
3. Seed arena chunk metadata (cursor/used/chunk-count at offsets 0x2c0.., see the
   =nelisp-standalone--arena-chunk-*-offset= consts) so the arena is well-formed
   for subsequent allocation (the image cursor = slen+1024).
4. INSTALL the EvalCtx roots: set env+8 = sstart+goff (globals Record),
   env+40 = sstart+foff, env+72 = sstart+uoff.  (Where -1 = immediate, rebuild
   inline per =bf_arena_image_root_verify=.)

** 3.2 New driver mode =exec-arena-image IMAGE FORM...=

In =nelisp-standalone--reader-driver-source= (line ~9708):
- Add matcher =nl_cstr_eq_exec_arena_image= for ="exec-arena-image"= (alongside
  the existing =nl_cstr_eq_exec_runtime_image= etc., ~lines 155–162 / 9866–9868).
- Add it to the mode-dispatch chains (3 sites: ~310–316, ~10021–10055,
  ~10689–10693).
- Handler: =nl_arena_init= (reserve real arena) → =bf_arena_boot_install(IMAGE)=
  (loads heap + installs env roots) → SKIP the normal bootstrap/prelude load (the
  image already contains the prelude + everything) → eval FORM through the normal
  eval path with the installed env → print.

Key invariant: the baked prelude must NOT be re-installed on top of the image
(the image already has it).  The handler replaces the boot, it does not augment.

* 4. Concept-proof plan (incremental, branch spike/flat-arena-boot-install)

1. *Scratch value-survival* — *DONE 2026-06-23*.  New builtin
   =nelisp--arena-value-survival PATH SYM= (=bf_arena_value_survival=): loads the
   image into scratch (c0 + ir), applies the split relocation (per
   =bf_arena_load_split_verify=), rebuilds the globals Record root (tag 12,
   box = c0+goff), and looks up SYM via =nelisp_mirror_lookup_entry= +
   =record-slot-ref= slot 0 (= the value cell; only mirror-lookup.o/entry is
   linked, so composed here, not =nelisp_mirror_lookup_value= which is not
   linked).  VERIFIED on target/nelisp: =(set 'ct 42)= → dump → survival → 42;
   strings ("hello"), conses ((1 . 2)), symbol lists ((a b c)) all survive; an
   unbound symbol misses → nil.  *Relocation correctness for arbitrary reachable
   state is now proven*; only the real-arena INSTALL (§3) remains.
2. *Real-arena install* — *DONE 2026-06-23, via the boot-wiring MARKER path (not the
   planned =exec-arena-image= driver mode)*.  The spike owner's boot-wiring already
   ships the real-arena install as a BOOT step gated by marker-file existence:
   - =nl_cold_load_arena= (build script ~10461): if =(nl_cold_marker_cpath)=
     (=/tmp/nelisp-cold-image.bin=) opens, loads the chunk-0 region into the REAL
     arena data area (=base+1024=) + interned region (=base+832=), applies
     =nl_cold_reloc= (table) + =nl_cold_reloc_intern= (interned base-delta), zeroes
     tail, clears marks, seeds the cursor (=base+0 = 1024+slen=, =base+840 = ib+isz=),
     pushes the next GC far, returns 1.  Runs BEFORE the boot alloc.
   - =nl_cold_overwrite_globals= (~10502): installs the globals Record into the
     EvalCtx (env+8 = tag-12 box at =ds+goff=).
   - Driver boot (~10456): =(_cl (nl_cold_load_arena))= then
     =(if (< _cl 0) <normal-boot> (nl_cold_overwrite_globals globals))=; the cold
     path SKIPS the baked prelude (the image already carries user state).
   *VERIFIED end-to-end* on =target/nelisp= (2026-06-23): process-1
   =(defun cf (x) (* x x)) (set 'k 5) (set 'lst '(10 20 30))
   (nelisp--arena-dump-image-to-file MARKER)= (image 47.9 MB) → process-2 boots
   with the marker present and evaluates =(list (fboundp 'cf) (cf 9) k lst)= →
   =(t 81 5 (10 20 30))=.  *A user-defined defun survives the cold-load AND is
   callable in the LIVE runtime; value + cons-list state survives.*  Removing the
   marker restores normal boot (=(+ 40 2)= → 42).
   - ✅ *Caveat FIXED in increment 4a (commit f781ce31)*: originally =(floor 7 2)= → 7
     on cold-load (native) vs 3 on normal boot (A1 prelude override); =funcall= also
     returned 7, and =(symbol-function 'floor)= had reverted to the native subr.  Root
     cause was NOT native dispatch but the boot order: the driver ran
     =nelisp-standalone--reader-install-builtins-forms= UNCONDITIONALLY at ~line 10505,
     AFTER =nl_cold_overwrite_globals= (10502) had installed the cold image's mirror.
     install-builtins re-registers every native builtin into the mirror, overwriting
     the image's entries — including the prelude overrides of those ~60 builtins — back
     to the bare native subr.  User symbols (=cf=) have no builtin entry, so they were
     never touched (which is why defuns survived but floor reverted).  *Fix*: gate the
     install-builtins splice on =(< _cl 0)= (normal boot only), mirroring the prelude
     gates.  The cold image already carries all native builtins + their overrides; the
     builtins' native code pointers are stable across processes (same static binary,
     same load base) and the override closures are relocated inside the image, so all
     remain callable.  *Verified*: cold =(floor 7 2)=3=, =(funcall #'floor 7 2)=3=,
     =(cf 9)=81=, =(+ 40 2)=42=, =(* 6 7)=42=, mapcar/eq/mod/truncate identical to
     normal boot.  No full-prelude re-run needed — that path is unsafe (free/reuse of
     image blocks → SIGSEGV, per the spike owner's prelude-skip comments).
3. *Real package over cold-load* — *PROVEN 2026-06-23 with the sibling
   =nelisp-emacs= repository's =src/seq.el=*.  The standalone reader can
   =(load "…/nelisp-emacs/src/seq.el")= directly (=load=/=require=/=provide=
   are present; =cl-defmethod=/=cl-defgeneric= are NOT, but this seq.el loads without
   them).  Flow: proc1 =(progn (load SEQ) (set 'pkg 'seq)
   (nelisp--arena-dump-image-to-file MARKER))= → 60 MB image; proc2 cold-loads (marker
   present) and evaluates =(list pkg (fboundp 'seq-filter)
   (seq-filter (lambda (x) (= 0 (mod x 2))) '(1 2 3 4 5 6)) (seq-map #'1+ '(10 20 30))
   (seq-reduce #'+ '(1 2 3 4) 0) (floor 7 2))= → =(seq t (2 4 6) (11 21 31) 10 3)=.
   *The loaded package survives the cold-load and is callable WITHOUT re-loading
   seq.el*; native-overrides survive too (=(floor 7 2)=3=).
   - *Timing (this lightweight reader)*: cold-load = *84 ms* vs reload (boot +
     =(load SEQ)=) = *219 ms* = *2.6× faster*, and even faster than a *bare* boot
     (183 ms) because the 4a install-builtins gate + the cold prelude-skip mean the
     image carries all builtins/prelude, so the cold path skips the entire boot
     install.  The win scales: a heavy library that costs minutes to load amortizes
     into a one-time dump + fast cold-loads.
   - *Caveat*: =%= is void (use =mod=); predicates must use available builtins.
   - *Vendor-core SUBSET extended + PROVEN 2026-06-23*: a 6-package subset
     (=subr-x= + =let-alist= + =cl-lib= + =emacs-pcase= + =map= + =seq=, all from
     =nelisp-emacs/src/=) loads cleanly into the reader in one process, dumps to a
     106 MB image, and on cold-load every driven function is *bit-identical* to the
     reload path: =(string-trim "  hi  ")="hi"=, =(cl-remove-if #'evenp '(1..6))=(1 3 5)=,
     =(seq-filter (> _ 2) '(1 2 3 4))=(3 4)=, =(map-elt '((a.1)(b.2)) 'b)=2=,
     =(pcase 5 ((pred integerp) 'int) …)=int=, =(let-alist '((x.10)(y.20)) (+ .x .y))=30=,
     =(floor 7 2)=3=.  *Timing*: cold-load *168 ms* vs reload (boot + 6× =load=)
     *333 ms* = ~2× (grows with library weight).
     - =rx.el= (1740 lines): *partially un-gapped 2026-06-23*.  Root cause of the
       earlier "loads but macro is inert" was =rx--to-expr= reading the special var
       =macroexpand-all-environment= while it was UNBOUND — an uncatchable abort in
       the reader (not even =condition-case= caught it).  Fix = =defvar= it nil +
       bake 7 general stdlib fns (characterp, max-char, mapcan, remq, memql,
       decode-char, regexp-opt) into the prelude (commit 6cd9ef6c).  rx now
       translates *literals / or / repetition (+,*,=N) / groups / anchors / named
       char-classes*: =(rx bol (group (+ word)) eol)= → ="^\\([[:word:]]+\\)$"=.
       =(any RANGE)= char-set translation *FIXED 2026-06-23 (Doc 158)*: the garbled
       =[^…]= complement was the reader's =<==/=<=/… comparison ops being 2-ary only
       (so =(<= 97 #x3fff7f 122)=t= flipped rx's complement test); =(any "abc")=
       aborted on a void =car-less-than-car= sort predicate.  Fixed natively (chained
       variadic comparison) + a baked =car-less-than-car=.  Now =(rx (any "a-z"))=
       ="[a-z]"=, =(any "abc")="[a-c]"=, =(not (any "a-z"))="[^a-z]"=.
     - =cl-generic.el= IS vendored (=vendor/emacs-lisp/emacs-lisp/cl-generic.el=) and
       *now loads fully* (2026-06-23): the chain was help-add-fundoc-usage (stubbed,
       6cd9ef6c) → pcase-let* (provided by emacs-pcase.el) → cl--generic-make.  That
       last one was the real find: cl-defstruct ignored MULTIPLE =:constructor=
       options, so =(:constructor nil)= masked =(:constructor cl--generic-make
       (name))= — fixed to process every cell (6cd9ef6c), which also exposed that
       native =recordp= rejected the vector-based =nelisp--make-record= (every
       struct predicate had regressed to nil since Doc-22 A14); fixed by building
       native records + a tag-12 =bf_aset= arm (56f0ae2b).
     - *cl-generic DISPATCH — the cl-function gap and far beyond (2026-06-23)*.  The
       chain past =cl-defmethod= expansion: =cl-function= (minimal `(function FUNC)`,
       baked 786a7a2d) → =macroexp--fgrep= → =help-split-fundoc= → =with-memoization=
       → =cl--find-class= (all baked, fboundp-gated, 786a7a2d) → the *setf/gv place*
       wall.  cl-generic does =(setf (cl--generic NAME) …)= (a MACRO place =`(get …)`),
       =(setf (alist-get …) …)=, and =(setf (cl--generic-dispatches g) …)= (a
       cl-defstruct accessor place).  The reader's two =setf= shims (prelude +
       cl-lib.el's standalone override, the latter winning once cl-lib loads) handled
       none of these.  Fixed both: prelude (786a7a2d) refactors place dispatch into
       =nelisp--setf-1= with get/gethash/alist-get/macro-place arms; cl-lib.el
       (nelisp-emacs 489ba45) adds alist-get + macro-place + the
       =nelisp-cl-macros--accessor-info= struct-accessor arm.  *Result*: loading
       =oclosure.el= + cl-lib + pcase + cl-generic, =cl-generic-define= now BUILDS the
       dispatch closure and =cl-defmethod= registers methods.
     - *cl--generic-nnm / oclosure gap — traced to the type lattice (2026-06-23)*.
       Dispatching a method reaches =(error cl--generic-nnm)=: =cl-generic-call-method=
       builds =(oclosure-lambda (cl--generic-nnm) …)=, and =oclosure-lambda= needs
       =(cl--find-class 'cl--generic-nnm)= (registered by the =(oclosure-define
       (cl--generic-nnm) …)= at cl-generic.el load).  First concrete gap *fixed*: class
       registration is =(setf (cl--find-class NAME) CLASS)=, an unsupported setf place
       — added =cl--set-find-class= (baked defun) + a =cl-simple-setter= registration
       in cl-lib.el (=put= must run from a *loaded file*: a top-level =put= in the
       AOT-baked prelude does NOT persist into the boot image — only definitions do).
       =(setf (cl--find-class X) c)= now works.  *Still the deep wall*: =oclosure.el=
       registers =(setf (cl--find-class 'oclosure) (oclosure--class-make … (list
       (cl--find-class 'closure)) …))= — and =(cl--find-class 'closure)= is nil because
       the base built-in class =closure= (and the whole =integer=/=t=/… lattice) is
       defined by =cl-preloaded.el= via =cl--define-built-in-type= / the
       =built-in-class= + =cl-structure-class= structs + =cl--struct-register-child=,
       all of which assume the Emacs C-core type infrastructure and do NOT come up
       standalone (cl-preloaded loads but registers nothing; =cl--define-built-in-type=
       ends up unbound).  So cl--generic-nnm is not a discrete gap — it bottoms out in
       *porting the Emacs built-in-class type lattice* to the reader.  *Done in
       Doc 157*: the lattice (cl--class/built-in-class + cl--class-allparents +
       cl--define-built-in-type + cl-type-of + the type hierarchy) is now live
       (the sibling =nelisp-emacs= repository's =src/cl-preloaded-shim.el= +
       cl-defstruct =:conc-name=/=:predicate=), and
       =oclosure-define= works.  cl-generic dispatch then advances to a *further*,
       separate wall: the reader's =(closure …)= LIST representation is incompatible
       with oclosure's =aref=-based closure-internals access (=(aref (lambda …) 0)=
       aborts) — a reader-core closure-representation item (Doc 157 §4).  rx
       =(any RANGE)= char-engine is the other open item.
     - =macroexp= (standalone) has no src file (=macroexpand-all= IS present); a
       remaining breadth gap.
     - Net: the cold-loader faithfully carries every package the reader can already
       load; growing the subset is now purely Doc-16 breadth work (fix the load/run
       gap, then it joins the dump for free).
4. *Full bundle* — *reframed 2026-06-23: the bundle is already fast via the .el
   load; the cold-loader is not needed for it.*  The "30–66 min / 11–15 GB" figure
   was the =.repl= path (=build/nemacs-bootstrap.repl=, form-by-form REPL replay).
   Loading =build/nemacs-bootstrap.el= (2.4 MB) instead with =(load …)= on
   =target/nelisp= takes *~12 s* and the full library is live + drivable:
   =(seq-filter …)=(2 3)=, =(cl-remove-if …)=(1 3)=, =(string-trim "  hi  ")="hi"=,
   =(mapcan …)=(1 1 2 2)=; cl-loop / pcase / gv-define-setter / rx / cl-defstruct /
   dolist / map-elt all fboundp.  So *full-bundle breadth needs no flat-arena image*
   — just =(load "build/nemacs-bootstrap.el")=.
   - The flat-arena DUMP of the full bundle *aborts* — and NOT for chunk-0 size:
     rebuilding with =NELISP_LINUX_ARENA_SIZE= = 1.5 GB and 4 GB still aborts the
     dump (the 95 MB 6-package subset dumps fine, §4.3).  It is a STRUCTURAL limit of
     the single-chunk cold-loader (fixed interned-region / relocation-table capacity
     sized for subset images), which would need a multi-chunk dump + larger interned
     region to capture the full library.  But that is *moot*: the ~12 s .el load
     already makes the bundle breadth-usable, so the cold-loader's role stays what
     §4.3 proved — fast reload of standalone-reader package SUBSETS (≤ ~95 MB,
     single chunk), not the whole bundle.

* 5. Risks / caveats

- The install writes the REAL arena before the eval loop runs — a relocation or
  metadata-seed error → SIGSEGV on the first variable lookup.  Debug with the
  scratch value-survival (step 1) FIRST to isolate relocation correctness from
  install correctness.
- Interned-region name-ptr base-delta (slot+8) is the most error-prone part
  (=bf_arena_load_split_verify= is the reference).
- This is the spike owner's active area (boot-wiring 5–14); coordinate to avoid
  duplicating the install they may already be drafting.
- The one-time dump still needs ONE full load; the flat-arena win is on RELOAD,
  not the first dump-creation.  The host OOM on the first load is orthogonal and
  unsolved (vendor-core subset mitigates by being lighter).
- *Cold-load + setf-on-struct-accessor edge* (2026-06-23): over a cold-loaded
  image, =(setf (STRUCT-SLOT r) v)= aborts in the setf-place macro expander
  (=(setf x …)=, =(setf (car l) …)=, =push= all work; the struct-accessor place is
  the only failure, fresh OR pre-dump record).  =(aset r i v)= and
  =nelisp--record-set= work directly over cold-load, so it is the gv/plist
  setf-expander survival, not record mutation.  READING cold-loaded structs
  (predicate + accessor) works, and normal-boot struct setf works fully — so
  breadth driving (read + call) is unaffected.  Likely a pre-existing cold-load
  limitation for plist-based macro expanders, surfaced only now that struct
  predicates work (Doc-22 A14 had silently broken them).

* 6. Status

Branch =spike/flat-arena-boot-install= created 2026-06-23.  Mechanism proven +
fully scoped here.  *Increment 1 (design)* + *increment 2 (scratch
value-survival, §4.1)* + *increment 3 (production INSTALL, §4.2)* DONE.

Increment 3 was satisfied by the spike owner's boot-wiring MARKER path (not the
planned =exec-arena-image= driver mode): =nl_cold_load_arena= +
=nl_cold_overwrite_globals= load + relocate + install the image into the REAL
arena/EvalCtx at boot, gated by =/tmp/nelisp-cold-image.bin= existence.  Verified
end-to-end: a user =defun= survives the cold-load and is callable (=（cf 9）→ 81=),
value + cons state survives.  KNOWN GAP (carry into increment 4): prelude
native-overrides (A1 floor etc.) revert to native because the cold path skips the
baked prelude — fix by re-applying the native-override subset after install.

*Increment 4a (native-override survival)* DONE (commit f781ce31) — the cold path
now skips install-builtins, so prelude overrides of native builtins survive
identically to a normal boot.  Cold-load is fully faithful.

*Increment 4b (real package over cold-load)* PROVEN with the sibling
=nelisp-emacs= repository's =src/seq.el= (2026-06-23):
the package loads into the reader, dumps to a 60 MB image, and on cold-load
seq-filter/seq-map/seq-reduce are callable WITHOUT re-loading, alongside surviving
native-overrides.  Cold-load 84 ms vs reload 219 ms (2.6×), faster than a bare boot.

*Increment 4b extended to a 6-package vendor-core SUBSET* (subr-x/let-alist/cl-lib/
pcase/map/seq) — DONE 2026-06-23: loads in one process → 106 MB image → cold-load
drives every package bit-identical to reload, 168 ms vs 333 ms (~2×).  =rx= loads but
its macro is a pre-existing reader gap (fails on normal boot too); =cl-generic=/
=macroexp= have no src and remain breadth gaps.  Growing the subset is now pure
Doc-16 breadth work — once a package loads/runs, it joins the dump for free.

Pending: increment 4c full bundle one-time dump (heavy, off-peak / high-RAM); fill
the rx / cl-generic breadth gaps to widen the subset.

* 7. Increments MC-1 / MC-2 / 3 (multi-chunk cold-load, 2026-07-04)

§4 Item 4 (2026-06-23) identified the single-chunk cold-loader's structural
ceiling: the flat-arena DUMP of anything past a single reserved chunk-0
aborted regardless of =NELISP_LINUX_ARENA_SIZE= (tried up to 4 GiB), because
both the relocation-table scratch (dump side) and the fixed
single-destination load target (install side) assumed a single chunk.  That
was "moot" for the full nemacs bundle only because
=(load "build/nemacs-bootstrap.el")= already makes that breadth usable in
~12 s without a cold-load image at all; it remained a real ceiling for
cold-loading anything the standalone reader itself builds past one chunk —
e.g. the ~60-file Org vendor chain used to drive/probe Org support, which
coalesces past 600 MiB across 7 chunks.  Branch
=feat/multi-chunk-cold-load= (worktree =nelisp.wt-coldload=) lifts this
ceiling in two increments, then Increment 3 built a reproducible
verification harness around them.

** MC-1: multi-chunk streaming dump (commit 5eb0fd81)

Root cause: =bf_arena_dump_image_stream= placed its relocation-table scratch
(the per-pointer-field offset table built during the in-place swizzle pass)
in whatever free tail happened to exist past the live bump cursor — chunk-0's
own mmap tail for a single-chunk heap, or the 64 MiB interned-symbol region's
unused tail for a multi-chunk heap.  Both placements are bounded by leftover
mmap space, not by the actual heap size, so a large heap overruns them and
writes relocation-table entries past the mapped region, causing a SIGSEGV.
This was never exercised at chunk-count > 1 before: a 7-chunk, ~602.5 MiB
heap (94% of a 640 MiB reservation, produced by loading the ~60-file Org
vendor chain) crashed both the streaming path (before this fix) and the
older =bf_arena_dump_image_to_file= (which still crashes, exit 139 — out of
scope for this increment).

Fix (=scripts/nelisp-standalone-build.el= ~3810-3886,
=bf_arena_dump_image_stream=): allocate a DEDICATED scratch mmap via
=nl_os_alloc_chunk= (the same platform-abstracted raw mmap
=nl_chunk_alloc_new= already uses for heap growth) instead of reusing
leftover space.  It is not linked into the chunk chain, so it stays
invisible to =nl_mc_write_chunks=/=nl_mc_total= and is never itself dumped
as heap data.  Size is a mathematical upper bound: at most one relocation
entry per 8-byte pointer-field word across the coalesced region.

** MC-2: multi-chunk boot-INSTALL (commit 1f486838)

Makes the boot-time cold-load INSTALL path (=nl_cold_load_arena= /
=nl_cold_overwrite_globals=) work with the multi-chunk coalesced images MC-1's
streaming dump now produces.  Previously the loader always targeted a single
fixed destination (=ds = base+1024= inside the fresh process's default
256 MiB chunk-0), which cannot hold a multi-hundred-MiB coalesced heap.

Destination strategy: read the image header first (header+8 = the coalesced
=slen=, format unchanged since MC-1) to compute the exact bytes needed —
1024-byte control block + =slen= + the relocation table (=tlen= * 8 bytes) +
8 MiB slack, 64 KiB aligned.  If that exceeds the current chunk-0's
reservation (=nl_cold_grow_chunk0=), allocate ONE new contiguous mmap via
=nl_os_alloc_chunk= sized to fit, point the driver-owned =nl_arena_base= bss
slot at it, and reinitialize its control block + chunk-0 descriptor via
=nl_cold_realloc_chunk0= — reusing the existing
=nelisp-standalone--arena-init-metadata-forms-dynamic= generator (the same
one =nl_arena_init= uses) with the new base/size as RUNTIME symbols instead
of a literal.  The interned-region pointer pair (+832/+840) is copied across
the swap; the OLD default chunk-0 mmap is deliberately left un-munmap'd (a
harmless orphaned mapping).  When the image already fits, the grow path is
skipped and the loader is byte-for-byte the original single-destination
code.

Also adds a =--cold-load-from PATH= CLI flag so callers/tests can cold-load
an explicit image without clobbering the fixed
=/tmp/nelisp-cold-image.bin= marker file.  This required moving argv parsing
earlier in =driver='s =let*=, ahead of the cold-load gate, so the gate can
recognize the flag before deciding whether/what to cold-load.  =PATH=
consumes the arg2 slot, so only arg3 remains free for
=--no-prompt=/=--no-print= (one fewer slot than plain =--repl=); the branch
otherwise runs the identical REPL-loop body.

** Increment 3 (2026-07-04): reproducible E2E harness + verification

Added =scripts/cold-image-org-e2e.sh= (+ its data file
=scripts/cold-image-org-e2e-files.txt=: the same 60-file Org vendor chain,
stored relative to =nelisp-emacs-lib='s =vendor/= root) — a single, checked-in,
reproducible script that: (a) generates the chain's =.repl= host-side via
=nelisp-emacs-lib='s =scripts/vendor-repl-standalone-replay.el='s
=vendor-repl-standalone--write-input= (read-only use of that sibling repo —
no source-normalize cache directory is configured, and all generated output
lands under a private =mktemp -d= workdir, never inside the sibling repo);
(b) replay-loads the =.repl= into this worktree's =./target/nelisp= and
dumps a multi-chunk image via =nelisp--arena-dump-image-stream=; (c)
cold-boots a FRESH process via =--cold-load-from= and evaluates the
faithfulness conjuncts; (d) prints a timing table (median of =N=
repetitions, default 3) and the replay-vs-cold-boot faithfulness columns
side by side.  Usage: =scripts/cold-image-org-e2e.sh [RUNS]=.

One implementation note folded into the script: this reader's REPL loop
(=nl_repl_read_line= / =nl_repl_loop=) reads and evaluates ONE OS line at a
time with no cross-line continuation, so every form fed to it (including the
script's own probe/dump/timing tail) must be a complete, self-balanced
expression on a single physical line.  The =.repl= chain =--write-input=
itself generates already satisfies this (per-form normalization keeps every
top-level form on one line; parenthesis-count false positives inside string
literals like regex patterns were the only lines that ever looked
"unbalanced" on inspection), so this only constrains the script's own
hand-written tail forms, which are written accordingly.

*** Timing (median of 3 runs, 2026-07-04)

| phase                                                 |  median | raw runs (us)           |
|--------------------------------------------------------+---------+--------------------------|
| full =.repl= replay load                               | 9.843 s | 9837679 9842727 9866091 |
| dump (=nelisp--arena-dump-image-stream=, in-process)    | 0.718 s | 718311 717912 720396    |
| cold boot to first eval (=--cold-load-from=)            | 0.853 s | 848855 853759 852653    |

Speedup (replay load / cold boot) = *11.5x*.  Cold-booted process peak RSS
(=/proc/PID/status= VmHWM, polled): *~695.3 MiB* (711976-711980 KB, stable
across all 3 runs).  Coalesced image size: ~728,000,000 bytes (728,075,872 B
this run; varies by a few hundred bytes run-to-run within the SAME script
invocation's 3 dump repeats it was byte-identical, but drifts slightly
between separate script invocations — not chased further, well within the
"~602.5 MiB heap / ~662-728 MB image" range already established by MC-1).

*** Faithfulness columns (Increment-3 proof-gap check)

Both a replay-loaded process and a cold-booted process (from the SAME image)
are probed with the identical conjunct list, evaluated BARE (the =commandp=
conjunct is guarded with =(and (fboundp 'commandp) ...)= because =commandp=
is a shim defined by one of the 60 vendor files — =(fboundp 'commandp)= is
=nil= at bare boot and becomes =t= only after the Org chain loads).
Columns re-measured 2026-07-04 after the probe fix described below
(=RUNS=1=: replay load 9.843 s, dump 0.686 s, cold boot 0.830 s, cold-boot
peak RSS 679112 KB, image 694418472 B, speedup 11.9x):

| conjunct                              | replay-loaded | cold-booted |
|----------------------------------------+---------------+-------------|
| =(featurep 'org)=                      | t             | t           |
| =(featurep 'org-element)=              | t             | t           |
| =(fboundp 'org-mode)=                  | nil           | nil         |
| =(fboundp 'org-element-parse-buffer)=  | t             | t           |
| =(commandp 'org-mode)=                 | nil           | nil         |
| =(fboundp 'commandp)=                  | t             | t           |

*Columns are IDENTICAL* — cold-load reproduces exactly what the
=.repl=-driven replay produces, proving MC-1/MC-2 faithfulness.

*Caveat RESOLVED (2026-07-04, branch =fix/e2e-repl-gate-parity=)*: the
first Increment-3 run of this harness reported ALL of the featurep/fboundp
conjuncts as nil (only the unwrapped =(fboundp 'commandp)= showed =t=) and
recorded that as a ".repl-generation settings difference vs the make-level
Org gate to reconcile".  Diffing the two generation paths showed the
generated =.repl= was never the problem: the harness's =.repl= and the
gate's =VENDOR_REPL_KEEP_TEMP=t= canonical =.repl= contain the same
per-file forms, the same in-file =(provide (quote org))=, and the same
per-file provide-safety-net tail, and the harness replay does load the full
chain (60/60 files ok, marker =ok:org-element.el:count=60=).  The setting
that differed was the HARNESS'S OWN PROBE: every conjunct was wrapped in
=ignore-errors=, and this standalone reader's =ignore-errors= /
=condition-case= returns nil instead of the protected body's value
(=(ignore-errors 42)= → =nil=, =(ignore-errors t)= → =nil=, verified
directly against the replay-loaded process), so each wrapped conjunct
reported nil regardless of the actual feature state.  Probing the same
replay-loaded state with BARE conjuncts returns =(featurep 'org)=t= and
=(featurep 'org-element)=t= — matching the make gate.  The fix removes the
=ignore-errors= wrappers from both the replay probe and the cold-boot probe
(same conjuncts verbatim in both columns).

The two remaining nil conjuncts (=fboundp org-mode= / =commandp org-mode=)
are a separate, pre-existing gap vs the make-level gate's all-true proof,
and were isolated to the PRELUDE input, not to cold-load: the gate's
=vendor-repl-standalone-batch= defaults =VENDOR_REPL_PRELUDE= to
=NELISP_ROOT/scripts/nelisp-stdlib-prelude.el= with
=NELISP_ROOT=vendor/nelisp= (nelisp-emacs-lib's vendored nelisp mirror,
commit f1ef06e1), under which =org.el='s
=(define-derived-mode org-mode ...)= form evaluates (slowly — the reader
spends >170 s inside =org.el= alone, most of the gate's 242 s wall) and
installs =org-mode=; this harness pairs the reader with THIS worktree's
newer =scripts/nelisp-stdlib-prelude.el=, under which the whole chain
replays in ~10 s and =org.el='s other definitions install (=org-todo=,
=org-insert-heading=, =org-entry-get=, ... all fbound) but that single
=define-derived-mode org-mode= form does not.  The generated =.repl= line
for that form is byte-identical between the two paths — only the prelude
environment it evaluates in differs.  The harness deliberately keeps this
worktree's prelude (reader and prelude from the same commit, and a ~10 s
replay instead of ~4 min); chasing the =org-mode= bring-up difference
between the two preludes is out of scope here and orthogonal to MC-1/MC-2,
whose faithfulness statement is: cold-load is a FAITHFUL continuation of
whatever the replay produces — it does not itself introduce divergence.

*** Regression (final binary, target/nelisp @ commit 1f486838)

- =/tmp/gcr1.el= (=(mg 300000)= cons/string churn + GC) and =/tmp/gcr2.el=
  (=defun= + =garbage-collect=) via =--load=: *10/10* each.
- 4 make smokes (=standalone-reader-load-smoke=, =-fmt-smoke=,
  =-repl-smoke=, =-prelude-test=): *PASS*.
- REPL error-tolerance (=(this-fn-does-not-exist)= then =(+ 40 3)= via
  =--repl --no-prompt=): prints *43*.
- Normal boot (no marker file, no =--cold-load-from=) is unaffected
  throughout by any of the above.

No merge-blocking issues found.  The pre-existing org-mode/=commandp= gaps
are orthogonal to this branch's scope (the multi-chunk cold-load mechanism)
and are unchanged by it — replay and cold-load agree with each other either
way.

** Gate-prelude opt-in probe (2026-07-04, branch =feat/org-cold-image-gate-prelude=): caveat NOT closed

=scripts/cold-image-org-e2e.sh= gained three env-var opt-ins
(=E2E_BOOTSTRAP_REPL=, =E2E_PRELUDE=, =E2E_LOAD_TIMEOUT=; default behavior
unchanged) so the replay-load phase can be re-pointed at
nelisp-emacs-lib's =make diagnose-vendor-repl-replay= gate inputs instead of
this worktree's own, to test the Increment-3 hypothesis above: that the
=org-mode= install gap is purely a *prelude-text* difference, and swapping
in the gate's prelude (keeping this worktree's own reader binary and
=nemacs-bootstrap.repl=, which were already identical to the gate's
defaults) would close it.

One run (=RUNS=1=, =E2E_PRELUDE=$NELISP_LIB_ROOT/vendor/nelisp/scripts/nelisp-stdlib-prelude.el=,
=E2E_LOAD_TIMEOUT=900=, this worktree's reader/bootstrap-repl otherwise
unchanged) was executed end-to-end:

| phase                    |    result |
|--------------------------+-----------|
| host =.repl= generation  |    0.77 s |
| replay load              |  253.32 s |
| dump (in-process)        |    3.45 s |
| cold boot to first eval  |    4.43 s |
| cold-boot peak RSS       | ~3.69 GiB |
| image size               | ~3.78 GiB |

The replay-load slowdown (9.8 s -> 253.3 s) reproduces the magnitude Doc
156 §7 attributed to =org.el='s =(define-derived-mode org-mode ...)= form
under the gate prelude (">170 s inside org.el alone"), and both the image
and cold-boot peak RSS grew roughly 5.4x versus the default-prelude
baseline (694 MB / 679 MB -> 3.78 GiB / 3.69 GiB) -- consistent with a much
larger/more expensive parse or expansion state being retained.  However,
the faithfulness columns came back *unchanged* from the Increment-3
baseline for BOTH the replay-loaded and the cold-booted process:

| conjunct                    | replay (default prelude) | replay (gate prelude) | cold-boot (gate-prelude image) |
|------------------------------+---------------------------+------------------------+---------------------------------|
| =(featurep 'org)=            | t                         | t                      | t                               |
| =(featurep 'org-element)=    | t                         | t                      | t                               |
| =(fboundp 'org-mode)=        | nil                       | nil                    | nil                             |
| =(fboundp 'org-element-parse-buffer)= | t                | t                      | t                               |
| =(commandp 'org-mode)=       | nil                       | nil                    | nil                             |
| =(fboundp 'commandp)=        | t                         | t                      | t                               |

So the prelude-text swap alone does *not* reproduce the gate's reported
=org-mode= install.  A cheap follow-up probe against the already-dumped
image (no re-load needed -- just an extra =--cold-load-from= boot, ~4 s)
added more diagnostics: =(fboundp 'define-derived-mode)= = t and it IS a
macro (=(eq (car-safe (symbol-function 'define-derived-mode)) 'macro)= =
t, defined by =vendor/emacs-lisp/emacs-lisp/derived.el=, unchanged by the
prelude swap); =(fboundp 'outline-mode)= = t (the parent mode =org-mode=
derives from is present); =(boundp 'org-mode)= = nil; =(fboundp
'org-mode-map)= = t and =(boundp 'org-mode-map)= = t (both set, which is
itself an anomaly for what should be a plain keymap variable, not a
function); =(fboundp 'org-mode-abbrev-table)= = nil.  Neither stdout nor
stderr contained any error/signal text from the 253 s replay-load run (both
empty under =--repl --no-prompt --no-print=) -- =define-derived-mode='s
expansion runs to completion without a visible error, but the resulting
=org-mode= command is never =fset=.

*Root-cause reassessment*: Doc 156 §7 attributed the gap to "PRELUDE
difference" and named the gate's default prelude path as
=NELISP_ROOT/scripts/nelisp-stdlib-prelude.el= with
=NELISP_ROOT=vendor/nelisp= (nelisp-emacs-lib's vendored nelisp mirror).
That same =NELISP_ROOT= is *also* nelisp-emacs-lib's default reader binary
root (=NELISP_ROOT/target/nelisp=, per its Makefile's
=DEFAULT_NELISP_BIN=), i.e. the gate does not just feed a different prelude
*file* into an otherwise-identical reader -- it runs a *different reader
binary* to begin with.  Checked read-only: that vendored mirror
(=nelisp-emacs-lib/vendor/nelisp=) is at commit =f1ef06e1= ("fix(eval):
condition-case must not catch throw"), a commit that does not exist
anywhere in this checkout's (=dev/nelisp=) git history -- confirmed via
=git log --oneline -1 f1ef06e1= failing ("unknown revision") from this
repo.  The vendored mirror is a separately-diverged interpreter build, not
an older commit reachable from this checkout's current HEAD (=f89b346f=).
This experiment held that binary fixed (this worktree's own
=./target/nelisp=, current HEAD) and varied only the prelude *text*; the
result shows prelude text is not sufficient by itself, so the original
diagnosis conflated two uncontrolled variables (prelude text and reader
binary version).

*Status*: caveat is **NOT closed**.  Per the task's stop condition, no
reader-internals changes were attempted to chase this further (that would
mean either bisecting/porting whatever changed in this checkout's
interpreter between the =f1ef06e1=-lineage build and current HEAD that
affects this specific =define-derived-mode= interaction, or building the
official cold image with the gate's own, differently-built binary and
separately verifying that binary's arena/image format is compatible with
this checkout's =--cold-load-from= consumer -- both out of scope here and
deferred back for a decision).  The env-var opt-in infrastructure
(=E2E_BOOTSTRAP_REPL=/=E2E_PRELUDE=/=E2E_LOAD_TIMEOUT=) itself is retained:
it is what made this a controlled, one-line-diff experiment instead of a
guess, and it remains available (default-off) for the next investigation
pass.  The regression suite (4 make smokes; default
=scripts/cold-image-org-e2e.sh 1= with no env overrides) was re-verified
green after adding it, with faithfulness columns unchanged
(=(t t nil t nil t)= both columns, matching Increment 3 exactly).

*Resolution (2026-07-05): the make gate's =status=done= does NOT imply
=org-mode= is installed -- the proof mechanism itself is structurally
unable to report failure.*  Investigated with nelisp-emacs-lib's
=vendor/nelisp/target/nelisp= overwritten by this checkout's =main=
(=af38dcfa=) build, so gate and manual replication share one binary.
Re-running =make diagnose-vendor-repl-replay-fast= over the real 60-file
chain with =VENDOR_REPL_KEEP_TEMP=t= reproduced =status=done elapsed=244.24
exit=0= and a kept =.repl= whose sentinel read
=VENDOR-REPL-STANDALONE=ok=.  =nelisp-emacs-lib/scripts/vendor-repl-standalone-replay.el='s
=vendor-repl-standalone--write-input= (lines 278-297) emits the proof as
=(setq vendor-repl-proof-value (condition-case err <PROOF> (error (setq
vendor-repl-proof-error err) nil)))= followed by =(if vendor-repl-proof-value
(nl-write-file MARKER "...ok") (progn ... (nl-write-file MARKER
"...fail...")))=, both pretty-printed across several physical lines via
=vendor-repl-standalone--pretty-form= (lines 236-240, =pp-to-string=) --
unlike ordinary vendor-file forms, which are normalized through
=standalone-source-normalize-file-to-form-strings= specifically to stay on
one physical line.  This reader's =--repl= loop reads and evaluates ONE
physical line at a time with NO cross-line form continuation (reconfirmed
directly: a bare two-line form =(princ (+ 1<NEWLINE> 2))= silently produces
no output/effect at all), so the multi-line proof scaffold is shredded: the
=setq= of =vendor-repl-proof-value= never completes (it stays void for the
whole run -- confirmed via a probe below), and of the two write lines only
the "ok" one is a self-contained, literal-only, single-physical-line form,
so it fires as an unconditional independent top-level call every time this
tail is reached; the "fail" line depends on =vendor-repl-proof-detail= (set
by an equally-shredded multi-line =setq=), so it always references a void
variable, errors, and is silently swallowed -- it can never fire.  Feeding
that exact kept =.repl= VERBATIM back into the same binary, with bare
single-line probes appended before =,quit=, gave: =(featurep 'org)=t=,
=(featurep 'org-element)=t=, =(fboundp 'org-mode)=nil=, =(fboundp
'org-element-parse-buffer)=t=, =(commandp 'org-mode)=nil=, =(boundp
'org-mode)=nil=, =(boundp 'vendor-repl-proof-value)=nil= -- and the
sentinel this manual run rewrote was again =VENDOR-REPL-STANDALONE=ok=.  A
minimal isolated repro confirms the mechanism generically: replacing the
real conjuncts with a deliberately false =(and nil nil nil)= in the same
multi-line shape still writes the "ok" marker, while the "fail" marker file
is never created.  This resolves the contradiction in favor of Hypothesis
H1 (proof-evaluation-path bug in the harness, not a real org-mode
bring-up): the gate's Org proof line never depends on the load outcome, so
its =status=done= carries no evidential weight for =fboundp
'org-mode='/=commandp 'org-mode='; every prior "Org gate passed" claim
keyed on this proof form should be read as "60/60 files loaded without a
load-time error", not as "org-mode is installed".  H2 (other synthesized
forms between =org-element.el='s last form and the proof) contributes
nothing beyond the already-documented, harmless per-file provide
safety-net.  H3 (normalization-cache staleness) is not implicated: the
proof/detail text is generated by =vendor-repl-standalone--pretty-form=,
which bypasses the file-normalization/cache subsystem entirely, so cache
state (confirmed warm and version/state-matched for =org.el= during this
run) has no bearing on this bug.  No source changes were made under this
investigation; the concrete fix belongs to
=vendor-repl-standalone--write-input=/=--pretty-form= (route the proof and
detail forms through the same single-physical-line-safe normalization used
for vendor files, or make the reader's REPL loop support cross-line form
continuation) and is left for a follow-up change.

** v2 re-attempt (2026-07-04, branch =feat/org-cold-image-v2=): org-mode-inclusive image PROVEN, caveat CLOSED

Re-ran the org-cold-image E2E after an unrelated, independently-landed
substrate fix (nelisp-emacs-lib commit =bd6d06d=, "Fix define-derived-mode
on the standalone bootstrap path", merged to its =main= as =165b50c=):
this standalone reader silently dropped the invocation line of any
user-defined macro whose expansion body is a backquote template, and both
layers of the =define-derived-mode= bridge (=emacs-mode-builtins.el= ->
=emacs-mode.el='s =emacs-mode-define-derived-mode=) were backquote
templates, so every vendor =(define-derived-mode CHILD PARENT ...)= call —
=org-mode= included — silently vanished, leaving the parent keymap/hook
defvars installed but no mode command ever =fset=.  That is the actual
root cause this doc's H1 finding (proof-scaffold-never-evaluates) left
undiagnosed: H1 explained why the *gate's* =status=done= carried no
evidential weight, but the org-mode/=commandp= gap itself was real and
had a different, independent cause, unrelated to any prelude/gate-proof
difference (§7 "Gate-prelude opt-in probe" had already ruled out prelude
text as sufficient by itself).

*** Bootstrap freshness check (before re-running anything)

Per this task's own gating instruction, verified whether
nelisp-emacs-lib's =build/nemacs-bootstrap.repl= (the default
=E2E_BOOTSTRAP_REPL= input) actually reflected the =bd6d06d= fix before
trusting it.  The live =nelisp-emacs-lib= checkout is an actively-used
shared working tree (an external automation loop was observed flipping it
between =main= and =integ/org-agenda-daily= every 1-3 hours via the
reflog, mid-investigation), so rather than run =make
build-nelisp-bootstrap= against that moving target, a throwaway
=git clone --no-hardlinks --no-checkout= of it was made into a private
scratch dir, checked out to the exact pinned commit =bd6d06d=, and
rebuilt there.  The independently-rebuilt =.repl= was byte-identical
(same size, same md5) to the live checkout's =build/nemacs-bootstrap.repl=
(mtime 15:28, i.e. *older* than the =bd6d06d= commit timestamp 15:42 —
misleading by mtime alone, but content-verified current: the fix had been
applied to the working tree and the bundle regenerated locally *before*
being committed, so the untracked build artifact's content matched the
eventual commit the whole time).  No edits were made to nelisp-emacs-lib;
the pinned clone was used read-only, both to confirm freshness and — for
extra isolation from the live checkout's churn during the ~10 s replay-load
window — as the =NELISP_LIB_ROOT= for the first E2E run below (a second,
completely unmodified default run against the live checkout, see
Regression below, confirms the live path independently gives the same
result).

*** Result: DEFAULT prelude now installs org-mode — no gate-prelude fallback needed

=scripts/cold-image-org-e2e.sh 1= with =E2E_LOAD_TIMEOUT=900= and
=NELISP_LIB_ROOT= pointed at the pinned, fix-verified clone (this
worktree's own default =scripts/nelisp-stdlib-prelude.el=, i.e. the FAST
default prelude that previously left =org-mode= unfbound per Increment 3):

| phase                     |    result |
|----------------------------+-----------|
| host =.repl= generation    |   0.819 s |
| replay load                |   9.909 s |
| dump (in-process)          |   0.700 s |
| cold boot to first eval    |   0.876 s |
| cold-boot peak RSS         | 674624 KB |
| image size                 | 689762616 B (~658 MiB) |
| speedup (replay/cold)      |     11.3x |

Faithfulness columns (bare conjuncts, replay vs cold-boot — IDENTICAL):

| conjunct                              | replay-loaded | cold-booted |
|----------------------------------------+---------------+-------------|
| =(featurep 'org)=                      | t             | t           |
| =(featurep 'org-element)=              | t             | t           |
| =(fboundp 'org-mode)=                  | *t*           | *t*         |
| =(fboundp 'org-element-parse-buffer)=  | t             | t           |
| =(commandp 'org-mode)=                 | =(interactive)= | =(interactive)= |
| =(fboundp 'commandp)=                  | t             | t           |

The two conjuncts that were =nil= in every prior recording (Increment 3,
the gate-prelude probe, and the original Doc 156 §7 finding) are now
truthy in both columns, under the DEFAULT (fast, ~10 s) prelude — the
slower =E2E_PRELUDE=vendor/nelisp/scripts/nelisp-stdlib-prelude.el= gate
prelude (~250-400 s, ~3.8 GiB image) was *not needed* this time; the fix
was in the reader's macro-expansion path, not in prelude content, exactly
as the "Gate-prelude opt-in probe" section above had already narrowed it
down to (prelude-text swap alone did not reproduce the gate's claimed
install; the real defect was the reader dropping the bridge macro's own
invocation).

Note on the =commandp= conjunct's printed value: this reader's =commandp=
shim returns the function's =(interactive ...)= spec form on success
rather than a bare =t= (a pre-existing shim-implementation detail, not a
regression) — both columns agree and the value is non-nil, so the
=(and (fboundp 'commandp) (commandp 'org-mode))= conjunct is truthy,
satisfying "=org-mode= is a real, invocable command" the same way a bare
=t= would.

*** Liveness checks (post-cold-load, same image)

All bare, no =condition-case=/=ignore-errors= wrapping (per the
already-documented quirk that those return nil regardless of the
protected body's value on this reader):

- =(garbage-collect)= — returns a normal GC-stats list, no crash.
- =(make-string 64 ?x)= — returns the 64-char string correctly.
- fresh =(defun f9 () 42)= then =(f9)= — returns =42=.
- =(commandp 'org-mode)= — =(interactive)= (truthy, see above).
- =(fboundp 'org-mode)= — =t=.

Buffer-level smoke (=(with-temp-buffer (org-mode) ...)=) was also
attempted per this task's "report what's feasible" allowance, and is
reported honestly as NOT working cleanly: =(with-temp-buffer major-mode)=
already returns =nil= (even with no mode call at all, and even after an
explicit =(fundamental-mode)=), while =(default-value 'major-mode)=
correctly returns =fundamental-mode= — so per-buffer =major-mode= tracking
inside =with-temp-buffer= does not fall back to the default value the way
it should.  Separately, =(with-temp-buffer (org-mode) t)= itself returns
=nil= instead of =t=, i.e. invoking =(org-mode)= inside the buffer appears
to discard the =with-temp-buffer= body's subsequent return value (no
stderr output, no non-zero exit — silent, not a crash).  This is a
buffer/major-mode substrate gap orthogonal to the =define-derived-mode=
bridge fix and to MC-1/MC-2 cold-load faithfulness (both of which are
about whether =org-mode= is =fboundp=/=commandp=-true and whether cold-load
reproduces that faithfully, which it does); it is left unchased here as
out of scope, consistent with this doc's existing practice of not chasing
gaps beyond the caveat under investigation.

*** Regression

- =scripts/cold-image-org-e2e.sh 1= re-run with *no env overrides at all*
  (default =NELISP_LIB_ROOT=, default prelude, default 180 s timeout,
  against the live nelisp-emacs-lib checkout, not the pinned clone):
  identical result — load 9.915 s, dump 0.695 s, cold boot 0.863 s, peak
  RSS 676636 KB, image 691817320 B, faithfulness columns
  =(t t t t (interactive) t)= in BOTH columns.  This independently confirms
  the live checkout's =build/nemacs-bootstrap.repl= is fix-current (matches
  the pinned-clone freshness check above) and that the org-mode install is
  reproducible without any of the env-var overrides added in the prior
  probe.
- 4 make smokes (=standalone-reader-load-smoke=, =-fmt-smoke=,
  =-repl-smoke=, =-prelude-test=): *PASS* (=./target/nelisp= relinked
  cleanly, 107 units, from this checkout's current =main= tip).

*** Status: CLOSED

The org-mode-install caveat opened by Increment 3 and left unresolved by
the "Gate-prelude opt-in probe" section is **CLOSED**: an org-mode-inclusive
cold image is proven, with =fboundp org-mode= and =commandp org-mode= both
truthy in replay and cold-boot alike, at boot times unchanged from the
non-org-mode-fbound baseline (~9.9 s replay load / ~0.86-0.88 s cold boot
to first eval, ~11.3-11.5x speedup) — i.e. closing the gap cost nothing in
image size or boot time versus the prior (org-mode-less) proof, because the
fix was a reader-macro-expansion correctness fix, not a heavier prelude.
The remaining, separately-scoped gap is the buffer/major-mode substrate
behavior noted above, which does not block this doc's cold-load
faithfulness claim.
