;; substrate-parity accepted divergences -- generated, review before commit.
;; Regenerate with NELISP_SUBSTRATE_PARITY_WRITE=1 (see the make
;; target); do not hand-add keys to silence a finding.
;;
;; Each key is "INDEX\tSUBSTRATE\tBASELINE\tACTUAL" -- both sides
;; of the divergence, not just its existence.  If either value
;; changes the key stops matching, the old entry goes stale, and
;; the new shape comes back as an unaccepted finding instead of
;; being silently covered (the ns-gate lesson).
;;
;; :notes is an alist of key -> why THIS entry is accepted, and it
;; survives regeneration.
(:generated-at "2026-08-27" :reason "Regenerated from a reviewed tree." :notes (("20	host	CRASH:void-function:emacs-pid	1" . "GAP vs host Emacs, consistent across every NeLisp substrate that can run this form -- not a cross-substrate NeLisp divergence. Form 20 (`emacs-pid') in particular is void-function in EVERY NeLisp substrate alike (bare, --load, runtime-image, artifact, source-cache), so it is a missing-primitive backlog item versus host Emacs, not a parity defect between NeLisp entry points.") ("35	source-fallback	1	CRASH:void-function:defvaralias" . "KNOWN GAP, MEASURED UNFIXABLE BY THIS LEDGER'S OWN METHOD (2026-08-22, re-triaged after extending `nelisp--primitive-symbols' with the ten names + `string-bytes' this ledger's other 18 source-fallback keys named -- see the commit that took this ledger from 21 keys to 3): `defvaralias' IS a genuine plain function in host Elisp (ordinary evaluated arguments, a real `symbol-function', despite reading like special-form syntax at a call site), so it was added to `nelisp--primitive-symbols' (src/nelisp-eval.el) alongside the eleven that fixed clean. Measured directly with `eval-elisp-source' against a rebuilt binary, marker file present vs. removed: adding it DOES fix the identical crash in the sibling source-CACHE substrate (marker present), but source-fallback specifically (marker removed, the pure re-interpreted-from-.el path) still answers `void-function:defvaralias' -- `(fboundp 'defvaralias)' is false in that one bootstrap's ambient environment before `nelisp--install-primitives' ever runs its `(when (fboundp sym) ...)' copy guard, unlike every other name added alongside it, which is `fboundp' in both. Root cause not isolated further; not the same class as the other source-fallback entries this ledger used to carry (those were a shared, single-cause list omission), and not a defvaralias-is-a-special-form claim either -- that framing was tried and measured wrong before this note was written. Real, but small and unresolved; left as a KNOWN GAP rather than pursued further this pass.") ("39	source-fallback	1	CRASH:void-function:char-table-p" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("40	source-fallback	1	CRASH:void-function:make-char-table" . "KNOWN GAP (2026-08-22, exposed by the eval-elisp-source pure-source-fallback file-read fix -- see src/nelisp-core-fileio.el -- and, for six of these forms, by the separate bare-file prelude-unification fix that stopped masking them behind a matching bare-side crash): source-fallback is the only substrate whose FORM arguments (and its loaded FILE's own top-level forms) run through the self-hosted `nelisp-eval' rather than through the native evaluator every other substrate calls directly. `nelisp--primitive-symbols' (src/nelisp-eval.el) is the list `nelisp--install-primitives' bulk-copies from native `fboundp' cells into `nelisp-eval's own function table, and it omits `floor', `%', `unibyte-string', `match-data', `nelisp--write-stdout-bytes', `intern-soft', `read-from-string', `read', `make-temp-name', and `prin1' -- all present natively (every other substrate agrees on the corpus's intended value), void-function (or `fboundp'-false) only where `nelisp-eval' has to look them up itself. Not fixed here: completing `nelisp--primitive-symbols' is a separate, broader prelude-parity sweep, not a defect in either the file-loading mechanism or the bare-file entry point this tree's two prelude fixes repair.") ("41	source-fallback	1	CRASH:void-function:nil" . "NEW GAP surfaced by Doc 191 Phase 1, renumbered from this branch's own form 39 to 41 at integration/wave4 merge 7/9 (feat/char-table-elisp-layer already claimed 39/40) -- a DIFFERENT root cause than the coincidentally-identical key shape this ledger already carried for other forms' missing-primitive gaps (see form 35's note): `symbol-function' IS on `nelisp--primitive-symbols' (src/nelisp-eval.el, alongside `fset', with its own comment calling it a bootstrap escape hatch that delegates to the HOST Emacs primitive). source-fallback is the one substrate where a `defun' inside the probed FORM is evaluated by the self-hosted `nelisp-eval' interpreter, which installs the new function into NeLisp's OWN `nelisp--functions' hash table, not into the symbol's real host Emacs function cell. Form 41's `(symbol-function (quote nelisp-parity-redef-fn))' then delegates to HOST `symbol-function', which correctly reports nil for a symbol whose host function cell was never `fset' -- `(funcall nil)' on that nil is what produces `void-function: nil'. Every OTHER substrate resolves the same call through NeLisp's own dispatch and sees the just-`defun'd closure. Not a NeLisp defect in the case-list semantics Doc 191 Phase 1 verifies (every non-source-fallback substrate, including host Emacs, agrees on baseline=1); a source-fallback-specific consequence of `symbol-function' being a host-delegating escape hatch rather than a NeLisp-native lookup, out of scope for Doc 191 to fix.") ("43	source-fallback	1	CRASH:nelisp: eval-elisp-source: NeLisp: symbol’s value is void: most-positive-fixnum" . "KNOWN GAP, Doc 190 Phase A (2026-08-23): same root-cause CLASS as forms 35/39/40/41 above -- source-fallback is the substrate whose FORM arguments run through the self-hosted `nelisp-eval' rather than the native evaluator every other substrate calls directly -- but a DIFFERENT specific gap: `most-positive-fixnum' is a VARIABLE, not a function, so it is never covered by `nelisp--primitive-symbols' (src/nelisp-eval.el, a FUNCTION-table copy list) at all; it is bound natively (every other substrate, host Emacs included, agrees on the corpus's intended value 1) but `nelisp-eval''s own variable lookup never sees it, producing `symbol's value is void' rather than `void-function'. Not fixed here: binding fixnum-boundary constants into the self-hosted interpreter's own variable environment is the same class of separate, broader prelude-parity sweep forms 39/40's note already scopes this out of -- this doc's own remit is the bignum box type, not source-fallback substrate completeness.") ("45	source-fallback	1	CRASH:nelisp: eval-elisp-source: NeLisp: symbol’s value is void: most-positive-fixnum" . "KNOWN GAP, Doc 190 Phase B (2026-08-23): same root-cause and same class as form 43's own note above -- source-fallback is the substrate whose FORM arguments run through the self-hosted `nelisp-eval' rather than the native evaluator every other substrate calls directly, and `most-positive-fixnum' is a VARIABLE, not a function, so it is never covered by `nelisp--primitive-symbols' (src/nelisp-eval.el, a FUNCTION-table copy list) at all -- it is bound natively (every other substrate, host Emacs included, agrees on the corpus's intended value 1) but `nelisp-eval''s own variable lookup never sees it, producing `symbol's value is void' rather than `void-function'. This form checks the exact promoted sum, `(+ most-positive-fixnum 1)'. Not fixed here, same scope note as form 43: binding fixnum-boundary constants into the self-hosted interpreter's own variable environment is a separate, broader prelude-parity sweep, not this phase's own remit (the bignum arithmetic core, not source-fallback substrate completeness).") ("46	source-fallback	1	CRASH:nelisp: eval-elisp-source: NeLisp: symbol’s value is void: most-positive-fixnum" . "KNOWN GAP, Doc 190 Phase B (2026-08-23): same root-cause and same class as form 43's own note above -- source-fallback is the substrate whose FORM arguments run through the self-hosted `nelisp-eval' rather than the native evaluator every other substrate calls directly, and `most-positive-fixnum' is a VARIABLE, not a function, so it is never covered by `nelisp--primitive-symbols' (src/nelisp-eval.el, a FUNCTION-table copy list) at all -- it is bound natively (every other substrate, host Emacs included, agrees on the corpus's intended value 1) but `nelisp-eval''s own variable lookup never sees it, producing `symbol's value is void' rather than `void-function'. This form checks exact bignum*bignum (genuine multi-limb), `(* most-positive-fixnum most-positive-fixnum)'. Not fixed here, same scope note as form 43: binding fixnum-boundary constants into the self-hosted interpreter's own variable environment is a separate, broader prelude-parity sweep, not this phase's own remit (the bignum arithmetic core, not source-fallback substrate completeness).") ("47	source-fallback	1	CRASH:nelisp: eval-elisp-source: NeLisp: symbol’s value is void: most-positive-fixnum" . "KNOWN GAP, Doc 190 Phase B (2026-08-23): same root-cause and same class as form 43's own note above -- source-fallback is the substrate whose FORM arguments run through the self-hosted `nelisp-eval' rather than the native evaluator every other substrate calls directly, and `most-positive-fixnum' is a VARIABLE, not a function, so it is never covered by `nelisp--primitive-symbols' (src/nelisp-eval.el, a FUNCTION-table copy list) at all -- it is bound natively (every other substrate, host Emacs included, agrees on the corpus's intended value 1) but `nelisp-eval''s own variable lookup never sees it, producing `symbol's value is void' rather than `void-function'. This form checks the demotion fencepost, `(- (+ most-positive-fixnum 1) 1)'. Not fixed here, same scope note as form 43: binding fixnum-boundary constants into the self-hosted interpreter's own variable environment is a separate, broader prelude-parity sweep, not this phase's own remit (the bignum arithmetic core, not source-fallback substrate completeness).")) :keys ("20	host	CRASH:void-function:emacs-pid	1" "35	source-fallback	1	CRASH:void-function:defvaralias" "39	source-fallback	1	CRASH:void-function:char-table-p" "40	source-fallback	1	CRASH:void-function:make-char-table" "41	source-fallback	1	CRASH:void-function:nil" "43	source-fallback	1	CRASH:nelisp: eval-elisp-source: NeLisp: symbol’s value is void: most-positive-fixnum" "45	source-fallback	1	CRASH:nelisp: eval-elisp-source: NeLisp: symbol’s value is void: most-positive-fixnum" "46	source-fallback	1	CRASH:nelisp: eval-elisp-source: NeLisp: symbol’s value is void: most-positive-fixnum" "47	source-fallback	1	CRASH:nelisp: eval-elisp-source: NeLisp: symbol’s value is void: most-positive-fixnum"))
